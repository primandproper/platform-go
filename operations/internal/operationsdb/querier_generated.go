// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package operationsdb

import (
	"context"
)

// Querier is every query in this package, with one signature per query
// regardless of which dialect answers it.
//
// That is the point of this package: the params type, the row type, and the
// method signature are the same on every dialect, so nothing above this line
// knows or cares which database is underneath.
//
// # A note on the :execrows count
//
// MySQL reports rows *changed*: an UPDATE that sets a column to the value it
// already held affects zero rows there. Postgres and SQLite report rows
// *matched*, and count that same UPDATE as one.
//
// So a statement that gates on the count — treating zero as "not found" — is
// correct on two engines and wrong on the third. Either give it a predicate
// that discriminates, or set clientFoundRows=true in the MySQL DSN, which
// switches MySQL to matched semantics.
//
// # A note on empty lists
//
// A list parameter that is empty matches nothing, on every dialect: Postgres
// binds an empty array to `= ANY`, and the other two expand to `IN (NULL)` because
// `IN ()` is a syntax error there. Asking for the rows whose key is in an empty
// set gets no rows back, which is what the empty set says, so a caller does not
// have to guard the call.
//
// The negation is where they part company, and nothing below can warn you. An
// empty list makes `NOT IN (NULL)` never true, so it matches nothing, while
// Postgres's empty `<> ALL` is true and matches everything. Both readings are
// defensible and no shared signature can say which was meant — so test
// membership rather than its negation, and let the caller decide what an empty
// set means before it calls.
type Querier interface {
	// BeginOperation runs the :one query.
	BeginOperation(ctx context.Context, db DBTX, arg BeginOperationParams) (BeginOperationRow, error)
	// CreateOperation runs the :one query.
	CreateOperation(ctx context.Context, db DBTX, arg CreateOperationParams) (CreateOperationRow, error)
	// FinishOperation runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	FinishOperation(ctx context.Context, db DBTX, arg FinishOperationParams) (int64, error)
	// FinishOperationWithEveryUnitDone runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	FinishOperationWithEveryUnitDone(ctx context.Context, db DBTX, arg FinishOperationWithEveryUnitDoneParams) (int64, error)
	// GetOperation runs the :one query.
	GetOperation(ctx context.Context, db DBTX, arg GetOperationParams) (GetOperationRow, error)
	// GetOperationInScope runs the :one query.
	GetOperationInScope(ctx context.Context, db DBTX, arg GetOperationInScopeParams) (GetOperationInScopeRow, error)
	// GetOperations runs the :many query.
	GetOperations(ctx context.Context, db DBTX, arg GetOperationsParams) ([]GetOperationsRow, error)
	// ListOperations runs the :many query.
	ListOperations(ctx context.Context, db DBTX, arg ListOperationsParams) ([]ListOperationsRow, error)
	// ListOperationsDescending runs the :many query.
	ListOperationsDescending(ctx context.Context, db DBTX, arg ListOperationsDescendingParams) ([]ListOperationsDescendingRow, error)
	// ListStrandedOperations runs the :many query.
	ListStrandedOperations(ctx context.Context, db DBTX, arg ListStrandedOperationsParams) ([]ListStrandedOperationsRow, error)
	// ReapOperations runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ReapOperations(ctx context.Context, db DBTX, arg ReapOperationsParams) (int64, error)
	// RecordOperationProgress runs the :one query.
	RecordOperationProgress(ctx context.Context, db DBTX, arg RecordOperationProgressParams) (RecordOperationProgressRow, error)
	// ReleaseOperation runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ReleaseOperation(ctx context.Context, db DBTX, arg ReleaseOperationParams) (int64, error)
	// RequestOperationCancel runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RequestOperationCancel(ctx context.Context, db DBTX, arg RequestOperationCancelParams) (int64, error)
}
