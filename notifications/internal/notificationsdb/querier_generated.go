// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package notificationsdb

import (
	"context"
)

// Querier is every query in this package, with one signature per query
// regardless of which dialect answers it.
//
// That is the point of this package: the params type, the row type, and the
// method signature are the same on every dialect, so nothing above this line
// knows or cares which database is underneath.
//
// # A note on the :execrows count
//
// MySQL reports rows *changed*: an UPDATE that sets a column to the value it
// already held affects zero rows there. Postgres and SQLite report rows
// *matched*, and count that same UPDATE as one.
//
// So a statement that gates on the count — treating zero as "not found" — is
// correct on two engines and wrong on the third. Either give it a predicate
// that discriminates, or set clientFoundRows=true in the MySQL DSN, which
// switches MySQL to matched semantics.
//
// # A note on empty lists
//
// A list parameter that is empty matches nothing, on every dialect: Postgres
// binds an empty array to `= ANY`, and the other two expand to `IN (NULL)` because
// `IN ()` is a syntax error there. Asking for the rows whose key is in an empty
// set gets no rows back, which is what the empty set says, so a caller does not
// have to guard the call.
//
// The negation is where they part company, and nothing below can warn you. An
// empty list makes `NOT IN (NULL)` never true, so it matches nothing, while
// Postgres's empty `<> ALL` is true and matches everything. Both readings are
// defensible and no shared signature can say which was meant — so test
// membership rather than its negation, and let the caller decide what an empty
// set means before it calls.
type Querier interface {
	// ArchiveNotification runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ArchiveNotification(ctx context.Context, db DBTX, arg ArchiveNotificationParams) (int64, error)
	// CreateNotification runs the :exec query.
	CreateNotification(ctx context.Context, db DBTX, arg CreateNotificationParams) error
	// DeleteDeviceToken runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	DeleteDeviceToken(ctx context.Context, db DBTX, arg DeleteDeviceTokenParams) (int64, error)
	// GetArchivedNotification runs the :one query.
	GetArchivedNotification(ctx context.Context, db DBTX, arg GetArchivedNotificationParams) (GetArchivedNotificationRow, error)
	// GetDevice runs the :one query.
	GetDevice(ctx context.Context, db DBTX, arg GetDeviceParams) (GetDeviceRow, error)
	// GetDeviceByToken runs the :one query.
	GetDeviceByToken(ctx context.Context, db DBTX, arg GetDeviceByTokenParams) (GetDeviceByTokenRow, error)
	// GetNotification runs the :one query.
	GetNotification(ctx context.Context, db DBTX, arg GetNotificationParams) (GetNotificationRow, error)
	// ListDevices runs the :many query.
	ListDevices(ctx context.Context, db DBTX, arg ListDevicesParams) ([]ListDevicesRow, error)
	// ListDevicesByPrincipals runs the :many query.
	ListDevicesByPrincipals(ctx context.Context, db DBTX, arg ListDevicesByPrincipalsParams) ([]ListDevicesByPrincipalsRow, error)
	// ListDevicesDescending runs the :many query.
	ListDevicesDescending(ctx context.Context, db DBTX, arg ListDevicesDescendingParams) ([]ListDevicesDescendingRow, error)
	// ListNotifications runs the :many query.
	ListNotifications(ctx context.Context, db DBTX, arg ListNotificationsParams) ([]ListNotificationsRow, error)
	// ListNotificationsDescending runs the :many query.
	ListNotificationsDescending(ctx context.Context, db DBTX, arg ListNotificationsDescendingParams) ([]ListNotificationsDescendingRow, error)
	// ListUnreadNotifications runs the :many query.
	ListUnreadNotifications(ctx context.Context, db DBTX, arg ListUnreadNotificationsParams) ([]ListUnreadNotificationsRow, error)
	// ListUnreadNotificationsDescending runs the :many query.
	ListUnreadNotificationsDescending(ctx context.Context, db DBTX, arg ListUnreadNotificationsDescendingParams) ([]ListUnreadNotificationsDescendingRow, error)
	// MarkAllNotificationsRead runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	MarkAllNotificationsRead(ctx context.Context, db DBTX, arg MarkAllNotificationsReadParams) (int64, error)
	// MarkNotificationRead runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	MarkNotificationRead(ctx context.Context, db DBTX, arg MarkNotificationReadParams) (int64, error)
	// RegisterDevice runs the :exec query.
	RegisterDevice(ctx context.Context, db DBTX, arg RegisterDeviceParams) error
	// RevokeDevice runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RevokeDevice(ctx context.Context, db DBTX, arg RevokeDeviceParams) (int64, error)
}
