-- Code generated by github.com/primandproper/platform-go/v14/comments/internal/queriesgen. DO NOT EDIT.

-- name: CreateComment :exec
INSERT INTO comments (
	id,
	scope,
	target_type,
	target_id,
	parent_id,
	author,
	body
) VALUES (
	sqlc.arg(id),
	sqlc.arg(scope),
	sqlc.arg(target_type),
	sqlc.arg(target_id),
	sqlc.arg(parent_id),
	sqlc.arg(author),
	sqlc.arg(body)
);

-- name: UpdateComment :execrows
UPDATE comments SET
	body = sqlc.arg(body),
	last_updated_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: ArchiveComment :execrows
UPDATE comments SET
	archived_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: DeleteCommentsForTarget :execrows
DELETE FROM comments
WHERE scope = sqlc.arg(scope)
	AND target_type = sqlc.arg(target_type)
	AND target_id = sqlc.arg(target_id);

-- name: DeleteCommentsByAuthor :execrows
DELETE FROM comments
WHERE scope = sqlc.arg(scope)
	AND author = sqlc.arg(author);

-- name: GetComment :one
SELECT
	comments.id,
	comments.scope,
	comments.target_type,
	comments.target_id,
	comments.parent_id,
	comments.author,
	comments.body,
	comments.created_at,
	comments.last_updated_at,
	comments.archived_at
FROM comments
WHERE comments.archived_at IS NULL
	AND comments.id = sqlc.arg(id)
	AND comments.scope = sqlc.arg(scope);

-- name: GetArchivedComment :one
SELECT
	comments.id,
	comments.scope,
	comments.target_type,
	comments.target_id,
	comments.parent_id,
	comments.author,
	comments.body,
	comments.created_at,
	comments.last_updated_at,
	comments.archived_at
FROM comments
WHERE comments.id = sqlc.arg(id)
	AND comments.scope = sqlc.arg(scope)
	AND comments.archived_at IS NOT NULL;

-- name: ListComments :many
SELECT
	comments.id,
	comments.scope,
	comments.target_type,
	comments.target_id,
	comments.parent_id,
	comments.author,
	comments.body,
	comments.created_at,
	comments.last_updated_at,
	comments.archived_at,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.target_type = sqlc.arg(target_type)
			AND comments.target_id = sqlc.arg(target_id)
			AND comments.parent_id = sqlc.arg(parent_id)
	) AS filtered_count,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.target_type = sqlc.arg(target_type)
			AND comments.target_id = sqlc.arg(target_id)
			AND comments.parent_id = sqlc.arg(parent_id)
	) AS total_count
FROM comments
WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
	AND comments.scope = sqlc.arg(scope)
	AND comments.target_type = sqlc.arg(target_type)
	AND comments.target_id = sqlc.arg(target_id)
	AND comments.parent_id = sqlc.arg(parent_id)
	AND comments.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY comments.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListCommentsDescending :many
SELECT
	comments.id,
	comments.scope,
	comments.target_type,
	comments.target_id,
	comments.parent_id,
	comments.author,
	comments.body,
	comments.created_at,
	comments.last_updated_at,
	comments.archived_at,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.target_type = sqlc.arg(target_type)
			AND comments.target_id = sqlc.arg(target_id)
			AND comments.parent_id = sqlc.arg(parent_id)
	) AS filtered_count,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.target_type = sqlc.arg(target_type)
			AND comments.target_id = sqlc.arg(target_id)
			AND comments.parent_id = sqlc.arg(parent_id)
	) AS total_count
FROM comments
WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
	AND comments.scope = sqlc.arg(scope)
	AND comments.target_type = sqlc.arg(target_type)
	AND comments.target_id = sqlc.arg(target_id)
	AND comments.parent_id = sqlc.arg(parent_id)
	AND (comments.id <= COALESCE(sqlc.narg(page_cursor), comments.id) AND comments.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY comments.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListCommentsByTargetType :many
SELECT
	comments.id,
	comments.scope,
	comments.target_type,
	comments.target_id,
	comments.parent_id,
	comments.author,
	comments.body,
	comments.created_at,
	comments.last_updated_at,
	comments.archived_at,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.target_type = sqlc.arg(target_type)
	) AS filtered_count,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.target_type = sqlc.arg(target_type)
	) AS total_count
FROM comments
WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
	AND comments.scope = sqlc.arg(scope)
	AND comments.target_type = sqlc.arg(target_type)
	AND comments.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY comments.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListCommentsByTargetTypeDescending :many
SELECT
	comments.id,
	comments.scope,
	comments.target_type,
	comments.target_id,
	comments.parent_id,
	comments.author,
	comments.body,
	comments.created_at,
	comments.last_updated_at,
	comments.archived_at,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.target_type = sqlc.arg(target_type)
	) AS filtered_count,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.target_type = sqlc.arg(target_type)
	) AS total_count
FROM comments
WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
	AND comments.scope = sqlc.arg(scope)
	AND comments.target_type = sqlc.arg(target_type)
	AND (comments.id <= COALESCE(sqlc.narg(page_cursor), comments.id) AND comments.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY comments.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListCommentsByAuthor :many
SELECT
	comments.id,
	comments.scope,
	comments.target_type,
	comments.target_id,
	comments.parent_id,
	comments.author,
	comments.body,
	comments.created_at,
	comments.last_updated_at,
	comments.archived_at,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.author = sqlc.arg(author)
	) AS filtered_count,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.author = sqlc.arg(author)
	) AS total_count
FROM comments
WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
	AND comments.scope = sqlc.arg(scope)
	AND comments.author = sqlc.arg(author)
	AND comments.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY comments.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListCommentsByAuthorDescending :many
SELECT
	comments.id,
	comments.scope,
	comments.target_type,
	comments.target_id,
	comments.parent_id,
	comments.author,
	comments.body,
	comments.created_at,
	comments.last_updated_at,
	comments.archived_at,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				comments.last_updated_at IS NULL
				OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.author = sqlc.arg(author)
	) AS filtered_count,
	(
		SELECT COUNT(comments.id)
		FROM comments
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
			AND comments.scope = sqlc.arg(scope)
			AND comments.author = sqlc.arg(author)
	) AS total_count
FROM comments
WHERE comments.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND comments.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		comments.last_updated_at IS NULL
		OR comments.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR comments.archived_at IS NULL)
	AND comments.scope = sqlc.arg(scope)
	AND comments.author = sqlc.arg(author)
	AND (comments.id <= COALESCE(sqlc.narg(page_cursor), comments.id) AND comments.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY comments.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);
