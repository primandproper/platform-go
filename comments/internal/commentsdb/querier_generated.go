// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package commentsdb

import (
	"context"
)

// Querier is every query in this package, with one signature per query
// regardless of which dialect answers it.
//
// That is the point of this package: the params type, the row type, and the
// method signature are the same on every dialect, so nothing above this line
// knows or cares which database is underneath.
//
// # A note on the :execrows count
//
// MySQL reports rows *changed*: an UPDATE that sets a column to the value it
// already held affects zero rows there. Postgres and SQLite report rows
// *matched*, and count that same UPDATE as one.
//
// So a statement that gates on the count — treating zero as "not found" — is
// correct on two engines and wrong on the third. Either give it a predicate
// that discriminates, or set clientFoundRows=true in the MySQL DSN, which
// switches MySQL to matched semantics.
type Querier interface {
	// ArchiveComment runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ArchiveComment(ctx context.Context, db DBTX, arg ArchiveCommentParams) (int64, error)
	// CreateComment runs the :exec query.
	CreateComment(ctx context.Context, db DBTX, arg CreateCommentParams) error
	// DeleteCommentsByAuthor runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	DeleteCommentsByAuthor(ctx context.Context, db DBTX, arg DeleteCommentsByAuthorParams) (int64, error)
	// DeleteCommentsForTarget runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	DeleteCommentsForTarget(ctx context.Context, db DBTX, arg DeleteCommentsForTargetParams) (int64, error)
	// GetArchivedComment runs the :one query.
	GetArchivedComment(ctx context.Context, db DBTX, arg GetArchivedCommentParams) (GetArchivedCommentRow, error)
	// GetComment runs the :one query.
	GetComment(ctx context.Context, db DBTX, arg GetCommentParams) (GetCommentRow, error)
	// ListComments runs the :many query.
	ListComments(ctx context.Context, db DBTX, arg ListCommentsParams) ([]ListCommentsRow, error)
	// ListCommentsByAuthor runs the :many query.
	ListCommentsByAuthor(ctx context.Context, db DBTX, arg ListCommentsByAuthorParams) ([]ListCommentsByAuthorRow, error)
	// ListCommentsByAuthorDescending runs the :many query.
	ListCommentsByAuthorDescending(ctx context.Context, db DBTX, arg ListCommentsByAuthorDescendingParams) ([]ListCommentsByAuthorDescendingRow, error)
	// ListCommentsByTargetType runs the :many query.
	ListCommentsByTargetType(ctx context.Context, db DBTX, arg ListCommentsByTargetTypeParams) ([]ListCommentsByTargetTypeRow, error)
	// ListCommentsByTargetTypeDescending runs the :many query.
	ListCommentsByTargetTypeDescending(ctx context.Context, db DBTX, arg ListCommentsByTargetTypeDescendingParams) ([]ListCommentsByTargetTypeDescendingRow, error)
	// ListCommentsDescending runs the :many query.
	ListCommentsDescending(ctx context.Context, db DBTX, arg ListCommentsDescendingParams) ([]ListCommentsDescendingRow, error)
	// UpdateComment runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	UpdateComment(ctx context.Context, db DBTX, arg UpdateCommentParams) (int64, error)
}
