-- Code generated by github.com/primandproper/platform-go/v14/issuereports/internal/queriesgen. DO NOT EDIT.

-- name: CreateReport :exec
INSERT INTO issue_reports (
	id,
	scope,
	reporter,
	kind,
	details,
	subject_type,
	subject_id,
	status,
	resolution,
	closed_at
) VALUES (
	sqlc.arg(id),
	sqlc.arg(scope),
	sqlc.arg(reporter),
	sqlc.arg(kind),
	sqlc.arg(details),
	sqlc.arg(subject_type),
	sqlc.arg(subject_id),
	sqlc.arg(status),
	sqlc.arg(resolution),
	sqlc.narg(closed_at)
);

-- name: UpdateReport :execrows
UPDATE issue_reports SET
	kind = sqlc.arg(kind),
	details = sqlc.arg(details),
	subject_type = sqlc.arg(subject_type),
	subject_id = sqlc.arg(subject_id),
	last_updated_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: TransitionReport :execrows
UPDATE issue_reports SET
	status = sqlc.arg(status),
	resolution = sqlc.arg(resolution),
	closed_at = sqlc.narg(closed_at),
	last_updated_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope)
	AND status = sqlc.arg(current_status);

-- name: ArchiveReport :execrows
UPDATE issue_reports SET
	archived_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: DeleteReportsByReporter :execrows
DELETE FROM issue_reports
WHERE scope = sqlc.arg(scope)
	AND reporter = sqlc.arg(reporter);

-- name: GetReport :one
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at
FROM issue_reports
WHERE issue_reports.archived_at IS NULL
	AND issue_reports.id = sqlc.arg(id)
	AND issue_reports.scope = sqlc.arg(scope);

-- name: GetArchivedReport :one
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at
FROM issue_reports
WHERE issue_reports.id = sqlc.arg(id)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.archived_at IS NOT NULL;

-- name: ListReports :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY issue_reports.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListReportsDescending :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND (issue_reports.id <= COALESCE(sqlc.narg(page_cursor), issue_reports.id) AND issue_reports.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY issue_reports.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListReportsByStatus :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.status = sqlc.arg(status)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.status = sqlc.arg(status)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.status = sqlc.arg(status)
	AND issue_reports.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY issue_reports.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListReportsByStatusDescending :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.status = sqlc.arg(status)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.status = sqlc.arg(status)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.status = sqlc.arg(status)
	AND (issue_reports.id <= COALESCE(sqlc.narg(page_cursor), issue_reports.id) AND issue_reports.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY issue_reports.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListReportsByReporter :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.reporter = sqlc.arg(reporter)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.reporter = sqlc.arg(reporter)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.reporter = sqlc.arg(reporter)
	AND issue_reports.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY issue_reports.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListReportsByReporterDescending :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.reporter = sqlc.arg(reporter)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.reporter = sqlc.arg(reporter)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.reporter = sqlc.arg(reporter)
	AND (issue_reports.id <= COALESCE(sqlc.narg(page_cursor), issue_reports.id) AND issue_reports.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY issue_reports.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListReportsBySubjectType :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.subject_type = sqlc.arg(subject_type)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.subject_type = sqlc.arg(subject_type)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.subject_type = sqlc.arg(subject_type)
	AND issue_reports.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY issue_reports.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListReportsBySubjectTypeDescending :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.subject_type = sqlc.arg(subject_type)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.subject_type = sqlc.arg(subject_type)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.subject_type = sqlc.arg(subject_type)
	AND (issue_reports.id <= COALESCE(sqlc.narg(page_cursor), issue_reports.id) AND issue_reports.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY issue_reports.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListReportsForSubject :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.subject_type = sqlc.arg(subject_type)
			AND issue_reports.subject_id = sqlc.arg(subject_id)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.subject_type = sqlc.arg(subject_type)
			AND issue_reports.subject_id = sqlc.arg(subject_id)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.subject_type = sqlc.arg(subject_type)
	AND issue_reports.subject_id = sqlc.arg(subject_id)
	AND issue_reports.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY issue_reports.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListReportsForSubjectDescending :many
SELECT
	issue_reports.id,
	issue_reports.scope,
	issue_reports.reporter,
	issue_reports.kind,
	issue_reports.details,
	issue_reports.subject_type,
	issue_reports.subject_id,
	issue_reports.status,
	issue_reports.resolution,
	issue_reports.closed_at,
	issue_reports.created_at,
	issue_reports.last_updated_at,
	issue_reports.archived_at,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
			)
			AND (
				issue_reports.last_updated_at IS NULL
				OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.subject_type = sqlc.arg(subject_type)
			AND issue_reports.subject_id = sqlc.arg(subject_id)
	) AS filtered_count,
	(
		SELECT COUNT(issue_reports.id)
		FROM issue_reports
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
			AND issue_reports.scope = sqlc.arg(scope)
			AND issue_reports.subject_type = sqlc.arg(subject_type)
			AND issue_reports.subject_id = sqlc.arg(subject_id)
	) AS total_count
FROM issue_reports
WHERE issue_reports.created_at > COALESCE(sqlc.narg(created_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	AND issue_reports.created_at < COALESCE(sqlc.narg(created_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT datetime(CURRENT_TIMESTAMP, '-999 years')))
	)
	AND (
		issue_reports.last_updated_at IS NULL
		OR issue_reports.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT datetime(CURRENT_TIMESTAMP, '+999 years')))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR issue_reports.archived_at IS NULL)
	AND issue_reports.scope = sqlc.arg(scope)
	AND issue_reports.subject_type = sqlc.arg(subject_type)
	AND issue_reports.subject_id = sqlc.arg(subject_id)
	AND (issue_reports.id <= COALESCE(sqlc.narg(page_cursor), issue_reports.id) AND issue_reports.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY issue_reports.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);
