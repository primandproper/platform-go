-- Code generated by github.com/primandproper/platform-go/v14/billing/internal/queriesgen. DO NOT EDIT.

-- name: GetProduct :one
SELECT
	billing_products.id,
	billing_products.scope,
	billing_products.name,
	billing_products.description,
	billing_products.kind,
	billing_products.amount_cents,
	billing_products.currency,
	billing_products.billing_interval_months,
	billing_products.external_product_id,
	billing_products.created_at,
	billing_products.last_updated_at,
	billing_products.archived_at
FROM billing_products
WHERE billing_products.archived_at IS NULL
	AND billing_products.id = sqlc.arg(id)
	AND billing_products.scope = sqlc.arg(scope);

-- name: CheckProductExistence :one
SELECT EXISTS (
	SELECT billing_products.id
	FROM billing_products
	WHERE billing_products.archived_at IS NULL
		AND billing_products.id = sqlc.arg(id)
		AND billing_products.scope = sqlc.arg(scope)
);

-- name: ListProducts :many
SELECT
	billing_products.id,
	billing_products.scope,
	billing_products.name,
	billing_products.description,
	billing_products.kind,
	billing_products.amount_cents,
	billing_products.currency,
	billing_products.billing_interval_months,
	billing_products.external_product_id,
	billing_products.created_at,
	billing_products.last_updated_at,
	billing_products.archived_at,
	(
		SELECT COUNT(billing_products.id)
		FROM billing_products
		WHERE billing_products.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_products.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_products.last_updated_at IS NULL
				OR billing_products.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_products.last_updated_at IS NULL
				OR billing_products.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_products.archived_at IS NULL)
			AND billing_products.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(billing_products.id)
		FROM billing_products
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_products.archived_at IS NULL)
			AND billing_products.scope = sqlc.arg(scope)
	) AS total_count
FROM billing_products
WHERE billing_products.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_products.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_products.last_updated_at IS NULL
		OR billing_products.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_products.last_updated_at IS NULL
		OR billing_products.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_products.archived_at IS NULL)
	AND billing_products.scope = sqlc.arg(scope)
	AND billing_products.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY billing_products.id ASC
LIMIT ?;

-- name: ListProductsDescending :many
SELECT
	billing_products.id,
	billing_products.scope,
	billing_products.name,
	billing_products.description,
	billing_products.kind,
	billing_products.amount_cents,
	billing_products.currency,
	billing_products.billing_interval_months,
	billing_products.external_product_id,
	billing_products.created_at,
	billing_products.last_updated_at,
	billing_products.archived_at,
	(
		SELECT COUNT(billing_products.id)
		FROM billing_products
		WHERE billing_products.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_products.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_products.last_updated_at IS NULL
				OR billing_products.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_products.last_updated_at IS NULL
				OR billing_products.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_products.archived_at IS NULL)
			AND billing_products.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(billing_products.id)
		FROM billing_products
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_products.archived_at IS NULL)
			AND billing_products.scope = sqlc.arg(scope)
	) AS total_count
FROM billing_products
WHERE billing_products.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_products.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_products.last_updated_at IS NULL
		OR billing_products.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_products.last_updated_at IS NULL
		OR billing_products.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_products.archived_at IS NULL)
	AND billing_products.scope = sqlc.arg(scope)
	AND (billing_products.id <= COALESCE(sqlc.narg(page_cursor), billing_products.id) AND billing_products.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY billing_products.id DESC
LIMIT ?;

-- name: UpdateProduct :execrows
UPDATE billing_products SET
	name = sqlc.arg(name),
	description = sqlc.arg(description),
	kind = sqlc.arg(kind),
	amount_cents = sqlc.arg(amount_cents),
	currency = sqlc.arg(currency),
	billing_interval_months = sqlc.narg(billing_interval_months),
	external_product_id = sqlc.narg(external_product_id),
	last_updated_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: ArchiveProduct :execrows
UPDATE billing_products SET
	archived_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: GetSubscription :one
SELECT
	billing_subscriptions.id,
	billing_subscriptions.scope,
	billing_subscriptions.belongs_to_account,
	billing_subscriptions.product_id,
	billing_subscriptions.external_subscription_id,
	billing_subscriptions.status,
	billing_subscriptions.current_period_start,
	billing_subscriptions.current_period_end,
	billing_subscriptions.created_at,
	billing_subscriptions.last_updated_at,
	billing_subscriptions.archived_at
FROM billing_subscriptions
WHERE billing_subscriptions.archived_at IS NULL
	AND billing_subscriptions.id = sqlc.arg(id)
	AND billing_subscriptions.scope = sqlc.arg(scope);

-- name: ListSubscriptions :many
SELECT
	billing_subscriptions.id,
	billing_subscriptions.scope,
	billing_subscriptions.belongs_to_account,
	billing_subscriptions.product_id,
	billing_subscriptions.external_subscription_id,
	billing_subscriptions.status,
	billing_subscriptions.current_period_start,
	billing_subscriptions.current_period_end,
	billing_subscriptions.created_at,
	billing_subscriptions.last_updated_at,
	billing_subscriptions.archived_at,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
	) AS total_count
FROM billing_subscriptions
WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
	AND billing_subscriptions.scope = sqlc.arg(scope)
	AND billing_subscriptions.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY billing_subscriptions.id ASC
LIMIT ?;

-- name: ListSubscriptionsDescending :many
SELECT
	billing_subscriptions.id,
	billing_subscriptions.scope,
	billing_subscriptions.belongs_to_account,
	billing_subscriptions.product_id,
	billing_subscriptions.external_subscription_id,
	billing_subscriptions.status,
	billing_subscriptions.current_period_start,
	billing_subscriptions.current_period_end,
	billing_subscriptions.created_at,
	billing_subscriptions.last_updated_at,
	billing_subscriptions.archived_at,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
	) AS total_count
FROM billing_subscriptions
WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
	AND billing_subscriptions.scope = sqlc.arg(scope)
	AND (billing_subscriptions.id <= COALESCE(sqlc.narg(page_cursor), billing_subscriptions.id) AND billing_subscriptions.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY billing_subscriptions.id DESC
LIMIT ?;

-- name: UpdateSubscription :execrows
UPDATE billing_subscriptions SET
	product_id = sqlc.arg(product_id),
	external_subscription_id = sqlc.narg(external_subscription_id),
	status = sqlc.arg(status),
	current_period_start = sqlc.arg(current_period_start),
	current_period_end = sqlc.arg(current_period_end),
	last_updated_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: ArchiveSubscription :execrows
UPDATE billing_subscriptions SET
	archived_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: GetPurchase :one
SELECT
	billing_purchases.id,
	billing_purchases.scope,
	billing_purchases.belongs_to_account,
	billing_purchases.product_id,
	billing_purchases.external_transaction_id,
	billing_purchases.amount_cents,
	billing_purchases.currency,
	billing_purchases.completed_at,
	billing_purchases.created_at,
	billing_purchases.last_updated_at,
	billing_purchases.archived_at
FROM billing_purchases
WHERE billing_purchases.archived_at IS NULL
	AND billing_purchases.id = sqlc.arg(id)
	AND billing_purchases.scope = sqlc.arg(scope);

-- name: ListPurchases :many
SELECT
	billing_purchases.id,
	billing_purchases.scope,
	billing_purchases.belongs_to_account,
	billing_purchases.product_id,
	billing_purchases.external_transaction_id,
	billing_purchases.amount_cents,
	billing_purchases.currency,
	billing_purchases.completed_at,
	billing_purchases.created_at,
	billing_purchases.last_updated_at,
	billing_purchases.archived_at,
	(
		SELECT COUNT(billing_purchases.id)
		FROM billing_purchases
		WHERE billing_purchases.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_purchases.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_purchases.last_updated_at IS NULL
				OR billing_purchases.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_purchases.last_updated_at IS NULL
				OR billing_purchases.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
			AND billing_purchases.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(billing_purchases.id)
		FROM billing_purchases
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
			AND billing_purchases.scope = sqlc.arg(scope)
	) AS total_count
FROM billing_purchases
WHERE billing_purchases.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_purchases.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_purchases.last_updated_at IS NULL
		OR billing_purchases.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_purchases.last_updated_at IS NULL
		OR billing_purchases.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
	AND billing_purchases.scope = sqlc.arg(scope)
	AND billing_purchases.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY billing_purchases.id ASC
LIMIT ?;

-- name: ListPurchasesDescending :many
SELECT
	billing_purchases.id,
	billing_purchases.scope,
	billing_purchases.belongs_to_account,
	billing_purchases.product_id,
	billing_purchases.external_transaction_id,
	billing_purchases.amount_cents,
	billing_purchases.currency,
	billing_purchases.completed_at,
	billing_purchases.created_at,
	billing_purchases.last_updated_at,
	billing_purchases.archived_at,
	(
		SELECT COUNT(billing_purchases.id)
		FROM billing_purchases
		WHERE billing_purchases.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_purchases.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_purchases.last_updated_at IS NULL
				OR billing_purchases.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_purchases.last_updated_at IS NULL
				OR billing_purchases.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
			AND billing_purchases.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(billing_purchases.id)
		FROM billing_purchases
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
			AND billing_purchases.scope = sqlc.arg(scope)
	) AS total_count
FROM billing_purchases
WHERE billing_purchases.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_purchases.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_purchases.last_updated_at IS NULL
		OR billing_purchases.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_purchases.last_updated_at IS NULL
		OR billing_purchases.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
	AND billing_purchases.scope = sqlc.arg(scope)
	AND (billing_purchases.id <= COALESCE(sqlc.narg(page_cursor), billing_purchases.id) AND billing_purchases.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY billing_purchases.id DESC
LIMIT ?;

-- name: ArchivePurchase :execrows
UPDATE billing_purchases SET
	archived_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: GetTransaction :one
SELECT
	billing_transactions.id,
	billing_transactions.scope,
	billing_transactions.belongs_to_account,
	billing_transactions.subscription_id,
	billing_transactions.purchase_id,
	billing_transactions.external_transaction_id,
	billing_transactions.status,
	billing_transactions.amount_cents,
	billing_transactions.currency,
	billing_transactions.created_at,
	billing_transactions.last_updated_at,
	billing_transactions.archived_at
FROM billing_transactions
WHERE billing_transactions.archived_at IS NULL
	AND billing_transactions.id = sqlc.arg(id)
	AND billing_transactions.scope = sqlc.arg(scope);

-- name: ListTransactions :many
SELECT
	billing_transactions.id,
	billing_transactions.scope,
	billing_transactions.belongs_to_account,
	billing_transactions.subscription_id,
	billing_transactions.purchase_id,
	billing_transactions.external_transaction_id,
	billing_transactions.status,
	billing_transactions.amount_cents,
	billing_transactions.currency,
	billing_transactions.created_at,
	billing_transactions.last_updated_at,
	billing_transactions.archived_at,
	(
		SELECT COUNT(billing_transactions.id)
		FROM billing_transactions
		WHERE billing_transactions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_transactions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_transactions.last_updated_at IS NULL
				OR billing_transactions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_transactions.last_updated_at IS NULL
				OR billing_transactions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
			AND billing_transactions.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(billing_transactions.id)
		FROM billing_transactions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
			AND billing_transactions.scope = sqlc.arg(scope)
	) AS total_count
FROM billing_transactions
WHERE billing_transactions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_transactions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_transactions.last_updated_at IS NULL
		OR billing_transactions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_transactions.last_updated_at IS NULL
		OR billing_transactions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
	AND billing_transactions.scope = sqlc.arg(scope)
	AND billing_transactions.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY billing_transactions.id ASC
LIMIT ?;

-- name: ListTransactionsDescending :many
SELECT
	billing_transactions.id,
	billing_transactions.scope,
	billing_transactions.belongs_to_account,
	billing_transactions.subscription_id,
	billing_transactions.purchase_id,
	billing_transactions.external_transaction_id,
	billing_transactions.status,
	billing_transactions.amount_cents,
	billing_transactions.currency,
	billing_transactions.created_at,
	billing_transactions.last_updated_at,
	billing_transactions.archived_at,
	(
		SELECT COUNT(billing_transactions.id)
		FROM billing_transactions
		WHERE billing_transactions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_transactions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_transactions.last_updated_at IS NULL
				OR billing_transactions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_transactions.last_updated_at IS NULL
				OR billing_transactions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
			AND billing_transactions.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(billing_transactions.id)
		FROM billing_transactions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
			AND billing_transactions.scope = sqlc.arg(scope)
	) AS total_count
FROM billing_transactions
WHERE billing_transactions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_transactions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_transactions.last_updated_at IS NULL
		OR billing_transactions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_transactions.last_updated_at IS NULL
		OR billing_transactions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
	AND billing_transactions.scope = sqlc.arg(scope)
	AND (billing_transactions.id <= COALESCE(sqlc.narg(page_cursor), billing_transactions.id) AND billing_transactions.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY billing_transactions.id DESC
LIMIT ?;

-- name: ArchiveTransaction :execrows
UPDATE billing_transactions SET
	archived_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: CreateProduct :execrows
INSERT IGNORE INTO billing_products (
	id,
	scope,
	name,
	description,
	kind,
	amount_cents,
	currency,
	billing_interval_months,
	external_product_id
) VALUES (
	sqlc.arg(id),
	sqlc.arg(scope),
	sqlc.arg(name),
	sqlc.arg(description),
	sqlc.arg(kind),
	sqlc.arg(amount_cents),
	sqlc.arg(currency),
	sqlc.narg(billing_interval_months),
	sqlc.narg(external_product_id)
);

-- name: CreateSubscription :execrows
INSERT IGNORE INTO billing_subscriptions (
	id,
	scope,
	belongs_to_account,
	product_id,
	external_subscription_id,
	status,
	current_period_start,
	current_period_end
) VALUES (
	sqlc.arg(id),
	sqlc.arg(scope),
	sqlc.arg(belongs_to_account),
	sqlc.arg(product_id),
	sqlc.narg(external_subscription_id),
	sqlc.arg(status),
	sqlc.arg(current_period_start),
	sqlc.arg(current_period_end)
);

-- name: CreatePurchase :execrows
INSERT IGNORE INTO billing_purchases (
	id,
	scope,
	belongs_to_account,
	product_id,
	external_transaction_id,
	amount_cents,
	currency,
	completed_at
) VALUES (
	sqlc.arg(id),
	sqlc.arg(scope),
	sqlc.arg(belongs_to_account),
	sqlc.arg(product_id),
	sqlc.narg(external_transaction_id),
	sqlc.arg(amount_cents),
	sqlc.arg(currency),
	sqlc.narg(completed_at)
);

-- name: CreateTransaction :execrows
INSERT IGNORE INTO billing_transactions (
	id,
	scope,
	belongs_to_account,
	subscription_id,
	purchase_id,
	external_transaction_id,
	status,
	amount_cents,
	currency
) VALUES (
	sqlc.arg(id),
	sqlc.arg(scope),
	sqlc.arg(belongs_to_account),
	sqlc.narg(subscription_id),
	sqlc.narg(purchase_id),
	sqlc.narg(external_transaction_id),
	sqlc.arg(status),
	sqlc.arg(amount_cents),
	sqlc.arg(currency)
);

-- name: CheckSubscriptionPresence :one
SELECT
	billing_subscriptions.id
FROM billing_subscriptions
WHERE billing_subscriptions.id = sqlc.arg(id)
	AND billing_subscriptions.scope = sqlc.arg(scope);

-- name: CheckPurchasePresence :one
SELECT
	billing_purchases.id
FROM billing_purchases
WHERE billing_purchases.id = sqlc.arg(id)
	AND billing_purchases.scope = sqlc.arg(scope);

-- name: GetProductCreatedAt :one
SELECT
	billing_products.created_at
FROM billing_products
WHERE billing_products.id = sqlc.arg(id);

-- name: GetSubscriptionCreatedAt :one
SELECT
	billing_subscriptions.created_at
FROM billing_subscriptions
WHERE billing_subscriptions.id = sqlc.arg(id);

-- name: GetPurchaseCreatedAt :one
SELECT
	billing_purchases.created_at
FROM billing_purchases
WHERE billing_purchases.id = sqlc.arg(id);

-- name: GetTransactionCreatedAt :one
SELECT
	billing_transactions.created_at
FROM billing_transactions
WHERE billing_transactions.id = sqlc.arg(id);

-- name: GetArchivedProduct :one
SELECT
	billing_products.id,
	billing_products.scope,
	billing_products.name,
	billing_products.description,
	billing_products.kind,
	billing_products.amount_cents,
	billing_products.currency,
	billing_products.billing_interval_months,
	billing_products.external_product_id,
	billing_products.created_at,
	billing_products.last_updated_at,
	billing_products.archived_at
FROM billing_products
WHERE billing_products.id = sqlc.arg(id)
	AND billing_products.scope = sqlc.arg(scope)
	AND billing_products.archived_at IS NOT NULL;

-- name: GetArchivedSubscription :one
SELECT
	billing_subscriptions.id,
	billing_subscriptions.scope,
	billing_subscriptions.belongs_to_account,
	billing_subscriptions.product_id,
	billing_subscriptions.external_subscription_id,
	billing_subscriptions.status,
	billing_subscriptions.current_period_start,
	billing_subscriptions.current_period_end,
	billing_subscriptions.created_at,
	billing_subscriptions.last_updated_at,
	billing_subscriptions.archived_at
FROM billing_subscriptions
WHERE billing_subscriptions.id = sqlc.arg(id)
	AND billing_subscriptions.scope = sqlc.arg(scope)
	AND billing_subscriptions.archived_at IS NOT NULL;

-- name: GetArchivedPurchase :one
SELECT
	billing_purchases.id,
	billing_purchases.scope,
	billing_purchases.belongs_to_account,
	billing_purchases.product_id,
	billing_purchases.external_transaction_id,
	billing_purchases.amount_cents,
	billing_purchases.currency,
	billing_purchases.completed_at,
	billing_purchases.created_at,
	billing_purchases.last_updated_at,
	billing_purchases.archived_at
FROM billing_purchases
WHERE billing_purchases.id = sqlc.arg(id)
	AND billing_purchases.scope = sqlc.arg(scope)
	AND billing_purchases.archived_at IS NOT NULL;

-- name: GetArchivedTransaction :one
SELECT
	billing_transactions.id,
	billing_transactions.scope,
	billing_transactions.belongs_to_account,
	billing_transactions.subscription_id,
	billing_transactions.purchase_id,
	billing_transactions.external_transaction_id,
	billing_transactions.status,
	billing_transactions.amount_cents,
	billing_transactions.currency,
	billing_transactions.created_at,
	billing_transactions.last_updated_at,
	billing_transactions.archived_at
FROM billing_transactions
WHERE billing_transactions.id = sqlc.arg(id)
	AND billing_transactions.scope = sqlc.arg(scope)
	AND billing_transactions.archived_at IS NOT NULL;

-- name: GetProductByExternalID :one
SELECT
	billing_products.id,
	billing_products.scope,
	billing_products.name,
	billing_products.description,
	billing_products.kind,
	billing_products.amount_cents,
	billing_products.currency,
	billing_products.billing_interval_months,
	billing_products.external_product_id,
	billing_products.created_at,
	billing_products.last_updated_at,
	billing_products.archived_at
FROM billing_products
WHERE billing_products.scope = sqlc.arg(scope)
	AND billing_products.external_product_id = sqlc.arg(external_product_id);

-- name: GetSubscriptionByExternalID :one
SELECT
	billing_subscriptions.id,
	billing_subscriptions.scope,
	billing_subscriptions.belongs_to_account,
	billing_subscriptions.product_id,
	billing_subscriptions.external_subscription_id,
	billing_subscriptions.status,
	billing_subscriptions.current_period_start,
	billing_subscriptions.current_period_end,
	billing_subscriptions.created_at,
	billing_subscriptions.last_updated_at,
	billing_subscriptions.archived_at
FROM billing_subscriptions
WHERE billing_subscriptions.scope = sqlc.arg(scope)
	AND billing_subscriptions.external_subscription_id = sqlc.arg(external_subscription_id);

-- name: GetPurchaseByExternalID :one
SELECT
	billing_purchases.id,
	billing_purchases.scope,
	billing_purchases.belongs_to_account,
	billing_purchases.product_id,
	billing_purchases.external_transaction_id,
	billing_purchases.amount_cents,
	billing_purchases.currency,
	billing_purchases.completed_at,
	billing_purchases.created_at,
	billing_purchases.last_updated_at,
	billing_purchases.archived_at
FROM billing_purchases
WHERE billing_purchases.scope = sqlc.arg(scope)
	AND billing_purchases.external_transaction_id = sqlc.arg(external_transaction_id);

-- name: GetTransactionByExternalID :one
SELECT
	billing_transactions.id,
	billing_transactions.scope,
	billing_transactions.belongs_to_account,
	billing_transactions.subscription_id,
	billing_transactions.purchase_id,
	billing_transactions.external_transaction_id,
	billing_transactions.status,
	billing_transactions.amount_cents,
	billing_transactions.currency,
	billing_transactions.created_at,
	billing_transactions.last_updated_at,
	billing_transactions.archived_at
FROM billing_transactions
WHERE billing_transactions.scope = sqlc.arg(scope)
	AND billing_transactions.external_transaction_id = sqlc.arg(external_transaction_id);

-- name: ListSubscriptionsForAccount :many
SELECT
	billing_subscriptions.id,
	billing_subscriptions.scope,
	billing_subscriptions.belongs_to_account,
	billing_subscriptions.product_id,
	billing_subscriptions.external_subscription_id,
	billing_subscriptions.status,
	billing_subscriptions.current_period_start,
	billing_subscriptions.current_period_end,
	billing_subscriptions.created_at,
	billing_subscriptions.last_updated_at,
	billing_subscriptions.archived_at,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
			AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS filtered_count,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
			AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS total_count
FROM billing_subscriptions
WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
	AND billing_subscriptions.scope = sqlc.arg(scope)
	AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
	AND billing_subscriptions.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY billing_subscriptions.id ASC
LIMIT ?;

-- name: ListSubscriptionsForAccountDescending :many
SELECT
	billing_subscriptions.id,
	billing_subscriptions.scope,
	billing_subscriptions.belongs_to_account,
	billing_subscriptions.product_id,
	billing_subscriptions.external_subscription_id,
	billing_subscriptions.status,
	billing_subscriptions.current_period_start,
	billing_subscriptions.current_period_end,
	billing_subscriptions.created_at,
	billing_subscriptions.last_updated_at,
	billing_subscriptions.archived_at,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
			AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS filtered_count,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
			AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS total_count
FROM billing_subscriptions
WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
	AND billing_subscriptions.scope = sqlc.arg(scope)
	AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
	AND (billing_subscriptions.id <= COALESCE(sqlc.narg(page_cursor), billing_subscriptions.id) AND billing_subscriptions.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY billing_subscriptions.id DESC
LIMIT ?;

-- name: ListCurrentSubscriptions :many
SELECT
	billing_subscriptions.id,
	billing_subscriptions.scope,
	billing_subscriptions.belongs_to_account,
	billing_subscriptions.product_id,
	billing_subscriptions.external_subscription_id,
	billing_subscriptions.status,
	billing_subscriptions.current_period_start,
	billing_subscriptions.current_period_end,
	billing_subscriptions.created_at,
	billing_subscriptions.last_updated_at,
	billing_subscriptions.archived_at,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
			AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
			AND billing_subscriptions.current_period_end > sqlc.arg(current_as_of)
	) AS filtered_count,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
			AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
			AND billing_subscriptions.current_period_end > sqlc.arg(current_as_of)
	) AS total_count
FROM billing_subscriptions
WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
	AND billing_subscriptions.scope = sqlc.arg(scope)
	AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
	AND billing_subscriptions.current_period_end > sqlc.arg(current_as_of)
	AND billing_subscriptions.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY billing_subscriptions.id ASC
LIMIT ?;

-- name: ListCurrentSubscriptionsDescending :many
SELECT
	billing_subscriptions.id,
	billing_subscriptions.scope,
	billing_subscriptions.belongs_to_account,
	billing_subscriptions.product_id,
	billing_subscriptions.external_subscription_id,
	billing_subscriptions.status,
	billing_subscriptions.current_period_start,
	billing_subscriptions.current_period_end,
	billing_subscriptions.created_at,
	billing_subscriptions.last_updated_at,
	billing_subscriptions.archived_at,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_subscriptions.last_updated_at IS NULL
				OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
			AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
			AND billing_subscriptions.current_period_end > sqlc.arg(current_as_of)
	) AS filtered_count,
	(
		SELECT COUNT(billing_subscriptions.id)
		FROM billing_subscriptions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
			AND billing_subscriptions.scope = sqlc.arg(scope)
			AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
			AND billing_subscriptions.current_period_end > sqlc.arg(current_as_of)
	) AS total_count
FROM billing_subscriptions
WHERE billing_subscriptions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_subscriptions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_subscriptions.last_updated_at IS NULL
		OR billing_subscriptions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_subscriptions.archived_at IS NULL)
	AND billing_subscriptions.scope = sqlc.arg(scope)
	AND billing_subscriptions.belongs_to_account = sqlc.arg(belongs_to_account)
	AND billing_subscriptions.current_period_end > sqlc.arg(current_as_of)
	AND (billing_subscriptions.id <= COALESCE(sqlc.narg(page_cursor), billing_subscriptions.id) AND billing_subscriptions.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY billing_subscriptions.id DESC
LIMIT ?;

-- name: ListPurchasesForAccount :many
SELECT
	billing_purchases.id,
	billing_purchases.scope,
	billing_purchases.belongs_to_account,
	billing_purchases.product_id,
	billing_purchases.external_transaction_id,
	billing_purchases.amount_cents,
	billing_purchases.currency,
	billing_purchases.completed_at,
	billing_purchases.created_at,
	billing_purchases.last_updated_at,
	billing_purchases.archived_at,
	(
		SELECT COUNT(billing_purchases.id)
		FROM billing_purchases
		WHERE billing_purchases.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_purchases.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_purchases.last_updated_at IS NULL
				OR billing_purchases.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_purchases.last_updated_at IS NULL
				OR billing_purchases.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
			AND billing_purchases.scope = sqlc.arg(scope)
			AND billing_purchases.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS filtered_count,
	(
		SELECT COUNT(billing_purchases.id)
		FROM billing_purchases
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
			AND billing_purchases.scope = sqlc.arg(scope)
			AND billing_purchases.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS total_count
FROM billing_purchases
WHERE billing_purchases.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_purchases.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_purchases.last_updated_at IS NULL
		OR billing_purchases.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_purchases.last_updated_at IS NULL
		OR billing_purchases.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
	AND billing_purchases.scope = sqlc.arg(scope)
	AND billing_purchases.belongs_to_account = sqlc.arg(belongs_to_account)
	AND billing_purchases.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY billing_purchases.id ASC
LIMIT ?;

-- name: ListPurchasesForAccountDescending :many
SELECT
	billing_purchases.id,
	billing_purchases.scope,
	billing_purchases.belongs_to_account,
	billing_purchases.product_id,
	billing_purchases.external_transaction_id,
	billing_purchases.amount_cents,
	billing_purchases.currency,
	billing_purchases.completed_at,
	billing_purchases.created_at,
	billing_purchases.last_updated_at,
	billing_purchases.archived_at,
	(
		SELECT COUNT(billing_purchases.id)
		FROM billing_purchases
		WHERE billing_purchases.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_purchases.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_purchases.last_updated_at IS NULL
				OR billing_purchases.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_purchases.last_updated_at IS NULL
				OR billing_purchases.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
			AND billing_purchases.scope = sqlc.arg(scope)
			AND billing_purchases.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS filtered_count,
	(
		SELECT COUNT(billing_purchases.id)
		FROM billing_purchases
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
			AND billing_purchases.scope = sqlc.arg(scope)
			AND billing_purchases.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS total_count
FROM billing_purchases
WHERE billing_purchases.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_purchases.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_purchases.last_updated_at IS NULL
		OR billing_purchases.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_purchases.last_updated_at IS NULL
		OR billing_purchases.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_purchases.archived_at IS NULL)
	AND billing_purchases.scope = sqlc.arg(scope)
	AND billing_purchases.belongs_to_account = sqlc.arg(belongs_to_account)
	AND (billing_purchases.id <= COALESCE(sqlc.narg(page_cursor), billing_purchases.id) AND billing_purchases.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY billing_purchases.id DESC
LIMIT ?;

-- name: ListTransactionsForAccount :many
SELECT
	billing_transactions.id,
	billing_transactions.scope,
	billing_transactions.belongs_to_account,
	billing_transactions.subscription_id,
	billing_transactions.purchase_id,
	billing_transactions.external_transaction_id,
	billing_transactions.status,
	billing_transactions.amount_cents,
	billing_transactions.currency,
	billing_transactions.created_at,
	billing_transactions.last_updated_at,
	billing_transactions.archived_at,
	(
		SELECT COUNT(billing_transactions.id)
		FROM billing_transactions
		WHERE billing_transactions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_transactions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_transactions.last_updated_at IS NULL
				OR billing_transactions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_transactions.last_updated_at IS NULL
				OR billing_transactions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
			AND billing_transactions.scope = sqlc.arg(scope)
			AND billing_transactions.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS filtered_count,
	(
		SELECT COUNT(billing_transactions.id)
		FROM billing_transactions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
			AND billing_transactions.scope = sqlc.arg(scope)
			AND billing_transactions.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS total_count
FROM billing_transactions
WHERE billing_transactions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_transactions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_transactions.last_updated_at IS NULL
		OR billing_transactions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_transactions.last_updated_at IS NULL
		OR billing_transactions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
	AND billing_transactions.scope = sqlc.arg(scope)
	AND billing_transactions.belongs_to_account = sqlc.arg(belongs_to_account)
	AND billing_transactions.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY billing_transactions.id ASC
LIMIT ?;

-- name: ListTransactionsForAccountDescending :many
SELECT
	billing_transactions.id,
	billing_transactions.scope,
	billing_transactions.belongs_to_account,
	billing_transactions.subscription_id,
	billing_transactions.purchase_id,
	billing_transactions.external_transaction_id,
	billing_transactions.status,
	billing_transactions.amount_cents,
	billing_transactions.currency,
	billing_transactions.created_at,
	billing_transactions.last_updated_at,
	billing_transactions.archived_at,
	(
		SELECT COUNT(billing_transactions.id)
		FROM billing_transactions
		WHERE billing_transactions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND billing_transactions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				billing_transactions.last_updated_at IS NULL
				OR billing_transactions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				billing_transactions.last_updated_at IS NULL
				OR billing_transactions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
			AND billing_transactions.scope = sqlc.arg(scope)
			AND billing_transactions.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS filtered_count,
	(
		SELECT COUNT(billing_transactions.id)
		FROM billing_transactions
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
			AND billing_transactions.scope = sqlc.arg(scope)
			AND billing_transactions.belongs_to_account = sqlc.arg(belongs_to_account)
	) AS total_count
FROM billing_transactions
WHERE billing_transactions.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND billing_transactions.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		billing_transactions.last_updated_at IS NULL
		OR billing_transactions.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		billing_transactions.last_updated_at IS NULL
		OR billing_transactions.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR billing_transactions.archived_at IS NULL)
	AND billing_transactions.scope = sqlc.arg(scope)
	AND billing_transactions.belongs_to_account = sqlc.arg(belongs_to_account)
	AND (billing_transactions.id <= COALESCE(sqlc.narg(page_cursor), billing_transactions.id) AND billing_transactions.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY billing_transactions.id DESC
LIMIT ?;

-- name: SetSubscriptionStatus :execrows
UPDATE billing_subscriptions SET
	status = sqlc.arg(status),
	last_updated_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope)
	AND status <> sqlc.arg(status);

-- name: SetTransactionStatus :execrows
UPDATE billing_transactions SET
	status = sqlc.arg(status),
	last_updated_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope)
	AND status <> sqlc.arg(status);

-- name: CompletePurchase :execrows
UPDATE billing_purchases SET
	completed_at = sqlc.narg(completed_at),
	last_updated_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope)
	AND completed_at IS NULL;
