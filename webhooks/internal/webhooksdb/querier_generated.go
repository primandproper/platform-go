// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package webhooksdb

import (
	"context"
)

// Querier is every query in this package, with one signature per query
// regardless of which dialect answers it.
//
// That is the point of this package: the params type, the row type, and the
// method signature are the same on every dialect, so nothing above this line
// knows or cares which database is underneath.
//
// # A note on the :execrows count
//
// MySQL reports rows *changed*: an UPDATE that sets a column to the value it
// already held affects zero rows there. Postgres and SQLite report rows
// *matched*, and count that same UPDATE as one.
//
// So a statement that gates on the count — treating zero as "not found" — is
// correct on two engines and wrong on the third. Either give it a predicate
// that discriminates, or set clientFoundRows=true in the MySQL DSN, which
// switches MySQL to matched semantics.
//
// # A note on empty lists
//
// A list parameter that is empty matches nothing, on every dialect: Postgres
// binds an empty array to `= ANY`, and the other two expand to `IN (NULL)` because
// `IN ()` is a syntax error there. Asking for the rows whose key is in an empty
// set gets no rows back, which is what the empty set says, so a caller does not
// have to guard the call.
//
// The negation is where they part company, and nothing below can warn you. An
// empty list makes `NOT IN (NULL)` never true, so it matches nothing, while
// Postgres's empty `<> ALL` is true and matches everything. Both readings are
// defensible and no shared signature can say which was meant — so test
// membership rather than its negation, and let the caller decide what an empty
// set means before it calls.
type Querier interface {
	// ArchiveEndpoint runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ArchiveEndpoint(ctx context.Context, db DBTX, arg ArchiveEndpointParams) (int64, error)
	// ArchiveSubscription runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ArchiveSubscription(ctx context.Context, db DBTX, arg ArchiveSubscriptionParams) (int64, error)
	// ArchiveSubscriptionByPair runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ArchiveSubscriptionByPair(ctx context.Context, db DBTX, arg ArchiveSubscriptionByPairParams) (int64, error)
	// ClaimDispatches runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ClaimDispatches(ctx context.Context, db DBTX, arg ClaimDispatchesParams) (int64, error)
	// DispatchBacklog runs the :one query.
	DispatchBacklog(ctx context.Context, db DBTX) (DispatchBacklogRow, error)
	// FetchClaimedDispatches runs the :many query.
	FetchClaimedDispatches(ctx context.Context, db DBTX, arg FetchClaimedDispatchesParams) ([]FetchClaimedDispatchesRow, error)
	// GetEndpoint runs the :one query.
	GetEndpoint(ctx context.Context, db DBTX, arg GetEndpointParams) (GetEndpointRow, error)
	// GetEndpointScope runs the :one query.
	GetEndpointScope(ctx context.Context, db DBTX, arg GetEndpointScopeParams) (GetEndpointScopeRow, error)
	// GetSubscription runs the :one query.
	GetSubscription(ctx context.Context, db DBTX, arg GetSubscriptionParams) (GetSubscriptionRow, error)
	// GetSubscriptionByPair runs the :one query.
	GetSubscriptionByPair(ctx context.Context, db DBTX, arg GetSubscriptionByPairParams) (GetSubscriptionByPairRow, error)
	// InsertAttempt runs the :exec query.
	InsertAttempt(ctx context.Context, db DBTX, arg InsertAttemptParams) error
	// InsertDelivery runs the :exec query.
	InsertDelivery(ctx context.Context, db DBTX, arg InsertDeliveryParams) error
	// InsertDispatch runs the :exec query.
	InsertDispatch(ctx context.Context, db DBTX, arg InsertDispatchParams) error
	// ListAttempts runs the :many query.
	ListAttempts(ctx context.Context, db DBTX, arg ListAttemptsParams) ([]ListAttemptsRow, error)
	// ListAttemptsDescending runs the :many query.
	ListAttemptsDescending(ctx context.Context, db DBTX, arg ListAttemptsDescendingParams) ([]ListAttemptsDescendingRow, error)
	// ListEndpoints runs the :many query.
	ListEndpoints(ctx context.Context, db DBTX, arg ListEndpointsParams) ([]ListEndpointsRow, error)
	// ListEndpointsDescending runs the :many query.
	ListEndpointsDescending(ctx context.Context, db DBTX, arg ListEndpointsDescendingParams) ([]ListEndpointsDescendingRow, error)
	// ListEndpointsForEvent runs the :many query.
	ListEndpointsForEvent(ctx context.Context, db DBTX, arg ListEndpointsForEventParams) ([]ListEndpointsForEventRow, error)
	// ListSubscriptions runs the :many query.
	ListSubscriptions(ctx context.Context, db DBTX, arg ListSubscriptionsParams) ([]ListSubscriptionsRow, error)
	// ListSubscriptionsDescending runs the :many query.
	ListSubscriptionsDescending(ctx context.Context, db DBTX, arg ListSubscriptionsDescendingParams) ([]ListSubscriptionsDescendingRow, error)
	// ListSubscriptionsForEndpoint runs the :many query.
	ListSubscriptionsForEndpoint(ctx context.Context, db DBTX, arg ListSubscriptionsForEndpointParams) ([]ListSubscriptionsForEndpointRow, error)
	// MarkDispatchDelivered runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	MarkDispatchDelivered(ctx context.Context, db DBTX, arg MarkDispatchDeliveredParams) (int64, error)
	// ReapAttempts runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ReapAttempts(ctx context.Context, db DBTX, arg ReapAttemptsParams) (int64, error)
	// ReapDeliveries runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ReapDeliveries(ctx context.Context, db DBTX, arg ReapDeliveriesParams) (int64, error)
	// ReapDispatches runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ReapDispatches(ctx context.Context, db DBTX, arg ReapDispatchesParams) (int64, error)
	// RecordDispatchFailure runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RecordDispatchFailure(ctx context.Context, db DBTX, arg RecordDispatchFailureParams) (int64, error)
	// RequeueDispatch runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RequeueDispatch(ctx context.Context, db DBTX, arg RequeueDispatchParams) (int64, error)
	// RotateEndpointSecret runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RotateEndpointSecret(ctx context.Context, db DBTX, arg RotateEndpointSecretParams) (int64, error)
	// SelectClaimableDispatches runs the :many query.
	SelectClaimableDispatches(ctx context.Context, db DBTX, arg SelectClaimableDispatchesParams) ([]SelectClaimableDispatchesRow, error)
	// UpsertEndpoint runs the :exec query.
	UpsertEndpoint(ctx context.Context, db DBTX, arg UpsertEndpointParams) error
	// UpsertSubscription runs the :exec query.
	UpsertSubscription(ctx context.Context, db DBTX, arg UpsertSubscriptionParams) error
}
