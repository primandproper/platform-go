-- Code generated by github.com/primandproper/platform-go/v14/authentication/grants/internal/queriesgen. DO NOT EDIT.

-- name: CreateGrant :exec
INSERT INTO oauth2_grants (
	id,
	scope,
	subject,
	provider,
	provider_account_id,
	granted_scopes,
	access_token,
	access_token_expires_at,
	refresh_token
) VALUES (
	sqlc.arg(id),
	sqlc.arg(scope),
	sqlc.arg(subject),
	sqlc.arg(provider),
	sqlc.arg(provider_account_id),
	sqlc.arg(granted_scopes),
	sqlc.arg(access_token),
	sqlc.narg(access_token_expires_at),
	sqlc.arg(refresh_token)
);

-- name: GetGrant :one
SELECT
	oauth2_grants.id,
	oauth2_grants.scope,
	oauth2_grants.subject,
	oauth2_grants.provider,
	oauth2_grants.provider_account_id,
	oauth2_grants.granted_scopes,
	oauth2_grants.access_token,
	oauth2_grants.access_token_expires_at,
	oauth2_grants.refresh_token,
	oauth2_grants.revocation_reason,
	oauth2_grants.created_at,
	oauth2_grants.last_updated_at,
	oauth2_grants.archived_at
FROM oauth2_grants
WHERE oauth2_grants.archived_at IS NULL
	AND oauth2_grants.id = sqlc.arg(id)
	AND oauth2_grants.scope = sqlc.arg(scope);

-- name: GetGrantForProvider :one
SELECT
	oauth2_grants.id,
	oauth2_grants.scope,
	oauth2_grants.subject,
	oauth2_grants.provider,
	oauth2_grants.provider_account_id,
	oauth2_grants.granted_scopes,
	oauth2_grants.access_token,
	oauth2_grants.access_token_expires_at,
	oauth2_grants.refresh_token,
	oauth2_grants.revocation_reason,
	oauth2_grants.created_at,
	oauth2_grants.last_updated_at,
	oauth2_grants.archived_at
FROM oauth2_grants
WHERE oauth2_grants.archived_at IS NULL
	AND oauth2_grants.scope = sqlc.arg(scope)
	AND oauth2_grants.subject = sqlc.arg(subject)
	AND oauth2_grants.provider = sqlc.arg(provider);

-- name: GetRevokedGrant :one
SELECT
	oauth2_grants.id,
	oauth2_grants.scope,
	oauth2_grants.subject,
	oauth2_grants.provider,
	oauth2_grants.provider_account_id,
	oauth2_grants.granted_scopes,
	oauth2_grants.access_token,
	oauth2_grants.access_token_expires_at,
	oauth2_grants.refresh_token,
	oauth2_grants.revocation_reason,
	oauth2_grants.created_at,
	oauth2_grants.last_updated_at,
	oauth2_grants.archived_at
FROM oauth2_grants
WHERE oauth2_grants.id = sqlc.arg(id)
	AND oauth2_grants.scope = sqlc.arg(scope)
	AND oauth2_grants.archived_at IS NOT NULL;

-- name: ListGrantsForSubjects :many
SELECT
	oauth2_grants.id,
	oauth2_grants.scope,
	oauth2_grants.subject,
	oauth2_grants.provider,
	oauth2_grants.provider_account_id,
	oauth2_grants.granted_scopes,
	oauth2_grants.access_token_expires_at,
	oauth2_grants.revocation_reason,
	oauth2_grants.created_at,
	oauth2_grants.last_updated_at,
	oauth2_grants.archived_at
FROM oauth2_grants
WHERE oauth2_grants.scope = sqlc.arg(scope)
	AND oauth2_grants.subject = ANY(sqlc.arg(subjects)::text[])
ORDER BY oauth2_grants.subject ASC, oauth2_grants.created_at ASC;

-- name: RefreshGrant :execrows
UPDATE oauth2_grants SET
	access_token = sqlc.arg(access_token),
	access_token_expires_at = sqlc.narg(access_token_expires_at),
	refresh_token = sqlc.arg(refresh_token),
	last_updated_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope)
	AND access_token = sqlc.arg(expected_access_token);

-- name: RevokeGrant :execrows
UPDATE oauth2_grants SET
	revocation_reason = sqlc.arg(revocation_reason),
	access_token = sqlc.arg(access_token),
	refresh_token = sqlc.arg(refresh_token),
	last_updated_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: ArchiveGrant :execrows
UPDATE oauth2_grants SET
	archived_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: DeleteGrantForProvider :execrows
DELETE FROM oauth2_grants
WHERE scope = sqlc.arg(scope)
	AND subject = sqlc.arg(subject)
	AND provider = sqlc.arg(provider);

-- name: DeleteGrantsForSubject :execrows
DELETE FROM oauth2_grants
WHERE scope = sqlc.arg(scope)
	AND subject = sqlc.arg(subject);
