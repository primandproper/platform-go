// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package grantsdb

import (
	"context"
	"strings"
	"time"

	"github.com/primandproper/primitives-go/v2/tenancy"
)

const archiveGrantSQLite = `UPDATE {{prefix}}oauth2_grants SET
	archived_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = ?1
	AND scope = ?2`

const createGrantSQLite = `
INSERT INTO {{prefix}}oauth2_grants (
	id,
	scope,
	subject,
	provider,
	provider_account_id,
	granted_scopes,
	access_token,
	access_token_expires_at,
	refresh_token
) VALUES (
	?1,
	?2,
	?3,
	?4,
	?5,
	?6,
	?7,
	?8,
	?9
)`

const deleteGrantForProviderSQLite = `DELETE FROM {{prefix}}oauth2_grants
WHERE scope = ?1
	AND subject = ?2
	AND provider = ?3`

const deleteGrantsForSubjectSQLite = `DELETE FROM {{prefix}}oauth2_grants
WHERE scope = ?1
	AND subject = ?2`

const getGrantSQLite = `SELECT
	{{prefix}}oauth2_grants.id,
	{{prefix}}oauth2_grants.scope,
	{{prefix}}oauth2_grants.subject,
	{{prefix}}oauth2_grants.provider,
	{{prefix}}oauth2_grants.provider_account_id,
	{{prefix}}oauth2_grants.granted_scopes,
	{{prefix}}oauth2_grants.access_token,
	{{prefix}}oauth2_grants.access_token_expires_at,
	{{prefix}}oauth2_grants.refresh_token,
	{{prefix}}oauth2_grants.revocation_reason,
	{{prefix}}oauth2_grants.created_at,
	{{prefix}}oauth2_grants.last_updated_at,
	{{prefix}}oauth2_grants.archived_at
FROM {{prefix}}oauth2_grants
WHERE {{prefix}}oauth2_grants.archived_at IS NULL
	AND {{prefix}}oauth2_grants.id = ?1
	AND {{prefix}}oauth2_grants.scope = ?2`

const getGrantForProviderSQLite = `SELECT
	{{prefix}}oauth2_grants.id,
	{{prefix}}oauth2_grants.scope,
	{{prefix}}oauth2_grants.subject,
	{{prefix}}oauth2_grants.provider,
	{{prefix}}oauth2_grants.provider_account_id,
	{{prefix}}oauth2_grants.granted_scopes,
	{{prefix}}oauth2_grants.access_token,
	{{prefix}}oauth2_grants.access_token_expires_at,
	{{prefix}}oauth2_grants.refresh_token,
	{{prefix}}oauth2_grants.revocation_reason,
	{{prefix}}oauth2_grants.created_at,
	{{prefix}}oauth2_grants.last_updated_at,
	{{prefix}}oauth2_grants.archived_at
FROM {{prefix}}oauth2_grants
WHERE {{prefix}}oauth2_grants.archived_at IS NULL
	AND {{prefix}}oauth2_grants.scope = ?1
	AND {{prefix}}oauth2_grants.subject = ?2
	AND {{prefix}}oauth2_grants.provider = ?3`

const getRevokedGrantSQLite = `SELECT
	{{prefix}}oauth2_grants.id,
	{{prefix}}oauth2_grants.scope,
	{{prefix}}oauth2_grants.subject,
	{{prefix}}oauth2_grants.provider,
	{{prefix}}oauth2_grants.provider_account_id,
	{{prefix}}oauth2_grants.granted_scopes,
	{{prefix}}oauth2_grants.access_token,
	{{prefix}}oauth2_grants.access_token_expires_at,
	{{prefix}}oauth2_grants.refresh_token,
	{{prefix}}oauth2_grants.revocation_reason,
	{{prefix}}oauth2_grants.created_at,
	{{prefix}}oauth2_grants.last_updated_at,
	{{prefix}}oauth2_grants.archived_at
FROM {{prefix}}oauth2_grants
WHERE {{prefix}}oauth2_grants.id = ?1
	AND {{prefix}}oauth2_grants.scope = ?2
	AND {{prefix}}oauth2_grants.archived_at IS NOT NULL`

const listGrantsForSubjectsSQLite = `SELECT
	{{prefix}}oauth2_grants.id,
	{{prefix}}oauth2_grants.scope,
	{{prefix}}oauth2_grants.subject,
	{{prefix}}oauth2_grants.provider,
	{{prefix}}oauth2_grants.provider_account_id,
	{{prefix}}oauth2_grants.granted_scopes,
	{{prefix}}oauth2_grants.access_token_expires_at,
	{{prefix}}oauth2_grants.revocation_reason,
	{{prefix}}oauth2_grants.created_at,
	{{prefix}}oauth2_grants.last_updated_at,
	{{prefix}}oauth2_grants.archived_at
FROM {{prefix}}oauth2_grants
WHERE {{prefix}}oauth2_grants.scope = ?1
	AND {{prefix}}oauth2_grants.subject IN (/*SLICE:subjects*/?)
ORDER BY {{prefix}}oauth2_grants.subject ASC, {{prefix}}oauth2_grants.created_at ASC`

const refreshGrantSQLite = `UPDATE {{prefix}}oauth2_grants SET
	access_token = ?1,
	access_token_expires_at = ?2,
	refresh_token = ?3,
	last_updated_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = ?4
	AND scope = ?5
	AND access_token = ?6`

const revokeGrantSQLite = `UPDATE {{prefix}}oauth2_grants SET
	revocation_reason = ?1,
	access_token = ?2,
	refresh_token = ?3,
	last_updated_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = ?4
	AND scope = ?5`

// sqliteQueries answers every query in Querier against sqlite.
type sqliteQueries struct {
	archiveGrant           string
	createGrant            string
	deleteGrantForProvider string
	deleteGrantsForSubject string
	getGrant               string
	getGrantForProvider    string
	getRevokedGrant        string
	listGrantsForSubjects  string
	refreshGrant           string
	revokeGrant            string
}

// newSQLite returns the sqlite querier with prefix substituted into every
// table name the analyzer identified.
func newSQLite(prefix string) *sqliteQueries {
	return &sqliteQueries{
		archiveGrant:           strings.ReplaceAll(archiveGrantSQLite, prefixMarker, prefix),
		createGrant:            strings.ReplaceAll(createGrantSQLite, prefixMarker, prefix),
		deleteGrantForProvider: strings.ReplaceAll(deleteGrantForProviderSQLite, prefixMarker, prefix),
		deleteGrantsForSubject: strings.ReplaceAll(deleteGrantsForSubjectSQLite, prefixMarker, prefix),
		getGrant:               strings.ReplaceAll(getGrantSQLite, prefixMarker, prefix),
		getGrantForProvider:    strings.ReplaceAll(getGrantForProviderSQLite, prefixMarker, prefix),
		getRevokedGrant:        strings.ReplaceAll(getRevokedGrantSQLite, prefixMarker, prefix),
		listGrantsForSubjects:  strings.ReplaceAll(listGrantsForSubjectsSQLite, prefixMarker, prefix),
		refreshGrant:           strings.ReplaceAll(refreshGrantSQLite, prefixMarker, prefix),
		revokeGrant:            strings.ReplaceAll(revokeGrantSQLite, prefixMarker, prefix),
	}
}

// timeText renders a time argument as the text this engine stores timestamps as.
//
// This engine has no date type: a DATETIME column holds text, and a comparison
// between two of them compares two strings. What a driver writes for a bound
// time.Time is Go's own rendering of it, whose leading characters happen to be
// the stored shape — but only while the value is UTC. A time carrying any other
// zone puts its own wall clock in those leading characters, so every window
// comparison is off by that offset, silently, and only for the callers whose
// clock is not UTC.
//
// Converting here is what makes the bound text the stored shape by
// construction rather than by accident, whatever zone the caller's time
// carries. The layout is whole seconds, which is what CURRENT_TIMESTAMP writes,
// so a bound value and a stored one are one shape rather than two that sort
// alike — which does mean a sub-second time bound here is stored truncated to
// the second on this engine.
func timeText(t time.Time) string {
	return t.UTC().Format("2006-01-02 15:04:05")
}

// timeTextPtr is timeText for a nullable argument. It preserves nil, so a
// field the caller left unset still binds NULL rather than the zero time.
func timeTextPtr(t *time.Time) any {
	if t == nil {
		return nil
	}

	return timeText(*t)
}

// ArchiveGrant runs the :execrows query against sqlite.
func (q *sqliteQueries) ArchiveGrant(ctx context.Context, db DBTX, arg ArchiveGrantParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.archiveGrant,
		arg.ID,
		arg.Scope,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// CreateGrant runs the :exec query against sqlite.
func (q *sqliteQueries) CreateGrant(ctx context.Context, db DBTX, arg CreateGrantParams) error {
	_, err := db.ExecContext(ctx, q.createGrant,
		arg.ID,
		arg.Scope,
		arg.Subject,
		arg.Provider,
		arg.ProviderAccountID,
		arg.GrantedScopes,
		arg.AccessToken,
		timeTextPtr(arg.AccessTokenExpiresAt),
		arg.RefreshToken,
	)

	return err
}

// DeleteGrantForProvider runs the :execrows query against sqlite.
func (q *sqliteQueries) DeleteGrantForProvider(ctx context.Context, db DBTX, arg DeleteGrantForProviderParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.deleteGrantForProvider,
		arg.Scope,
		arg.Subject,
		arg.Provider,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// DeleteGrantsForSubject runs the :execrows query against sqlite.
func (q *sqliteQueries) DeleteGrantsForSubject(ctx context.Context, db DBTX, arg DeleteGrantsForSubjectParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.deleteGrantsForSubject,
		arg.Scope,
		arg.Subject,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// GetGrant runs the :one query against sqlite.
func (q *sqliteQueries) GetGrant(ctx context.Context, db DBTX, arg GetGrantParams) (GetGrantRow, error) {
	row := db.QueryRowContext(ctx, q.getGrant,
		arg.ID,
		arg.Scope,
	)

	var i GetGrantRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.Subject,
		&i.Provider,
		&i.ProviderAccountID,
		&i.GrantedScopes,
		&i.AccessToken,
		&i.AccessTokenExpiresAt,
		&i.RefreshToken,
		&i.RevocationReason,
		&i.CreatedAt,
		&i.LastUpdatedAt,
		&i.ArchivedAt,
	)

	return i, err
}

// GetGrantForProvider runs the :one query against sqlite.
func (q *sqliteQueries) GetGrantForProvider(ctx context.Context, db DBTX, arg GetGrantForProviderParams) (GetGrantForProviderRow, error) {
	row := db.QueryRowContext(ctx, q.getGrantForProvider,
		arg.Scope,
		arg.Subject,
		arg.Provider,
	)

	var i GetGrantForProviderRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.Subject,
		&i.Provider,
		&i.ProviderAccountID,
		&i.GrantedScopes,
		&i.AccessToken,
		&i.AccessTokenExpiresAt,
		&i.RefreshToken,
		&i.RevocationReason,
		&i.CreatedAt,
		&i.LastUpdatedAt,
		&i.ArchivedAt,
	)

	return i, err
}

// GetRevokedGrant runs the :one query against sqlite.
func (q *sqliteQueries) GetRevokedGrant(ctx context.Context, db DBTX, arg GetRevokedGrantParams) (GetRevokedGrantRow, error) {
	row := db.QueryRowContext(ctx, q.getRevokedGrant,
		arg.ID,
		arg.Scope,
	)

	var i GetRevokedGrantRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.Subject,
		&i.Provider,
		&i.ProviderAccountID,
		&i.GrantedScopes,
		&i.AccessToken,
		&i.AccessTokenExpiresAt,
		&i.RefreshToken,
		&i.RevocationReason,
		&i.CreatedAt,
		&i.LastUpdatedAt,
		&i.ArchivedAt,
	)

	return i, err
}

// ListGrantsForSubjects runs the :many query against sqlite.
func (q *sqliteQueries) ListGrantsForSubjects(ctx context.Context, db DBTX, arg ListGrantsForSubjectsParams) ([]ListGrantsForSubjectsRow, error) {
	query := q.listGrantsForSubjects

	args := make([]any, 0, 1+len(arg.Subjects))

	args = append(args, arg.Scope)

	query = strings.Replace(query, "/*SLICE:subjects*/?", slicePlaceholders("?", len(arg.Subjects)), 1)

	for _, v := range arg.Subjects {
		args = append(args, v)
	}

	rows, err := db.QueryContext(ctx, query, args...)
	if err != nil {
		return nil, err
	}

	defer func() { _ = rows.Close() }()

	var items []ListGrantsForSubjectsRow

	for rows.Next() {
		var i ListGrantsForSubjectsRow

		if err := rows.Scan(
			&i.ID,
			&i.Scope,
			&i.Subject,
			&i.Provider,
			&i.ProviderAccountID,
			&i.GrantedScopes,
			&i.AccessTokenExpiresAt,
			&i.RevocationReason,
			&i.CreatedAt,
			&i.LastUpdatedAt,
			&i.ArchivedAt,
		); err != nil {
			return nil, err
		}

		items = append(items, i)
	}

	if err := rows.Err(); err != nil {
		return nil, err
	}

	return items, nil
}

// RefreshGrant runs the :execrows query against sqlite.
func (q *sqliteQueries) RefreshGrant(ctx context.Context, db DBTX, arg RefreshGrantParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.refreshGrant,
		arg.AccessToken,
		timeTextPtr(arg.AccessTokenExpiresAt),
		arg.RefreshToken,
		arg.ID,
		arg.Scope,
		arg.ExpectedAccessToken,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// RevokeGrant runs the :execrows query against sqlite.
func (q *sqliteQueries) RevokeGrant(ctx context.Context, db DBTX, arg RevokeGrantParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.revokeGrant,
		arg.RevocationReason,
		arg.AccessToken,
		arg.RefreshToken,
		arg.ID,
		arg.Scope,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// Shape assertions.
//
// Each conversion below compiles only if the shared type still has exactly
// these fields, with these types, in this order — which is what makes a
// disagreement between dialects a compile error here rather than a
// transposition at run time.
var (
	_ = struct {
		ID    string
		Scope tenancy.Scope
	}(ArchiveGrantParams{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
	}(CreateGrantParams{})
	_ = struct {
		Scope    tenancy.Scope
		Subject  string
		Provider string
	}(DeleteGrantForProviderParams{})
	_ = struct {
		Scope   tenancy.Scope
		Subject string
	}(DeleteGrantsForSubjectParams{})
	_ = struct {
		ID    string
		Scope tenancy.Scope
	}(GetGrantParams{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
		RevocationReason     string
		CreatedAt            time.Time
		LastUpdatedAt        *time.Time
		ArchivedAt           *time.Time
	}(GetGrantRow{})
	_ = struct {
		Scope    tenancy.Scope
		Subject  string
		Provider string
	}(GetGrantForProviderParams{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
		RevocationReason     string
		CreatedAt            time.Time
		LastUpdatedAt        *time.Time
		ArchivedAt           *time.Time
	}(GetGrantForProviderRow{})
	_ = struct {
		ID    string
		Scope tenancy.Scope
	}(GetRevokedGrantParams{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
		RevocationReason     string
		CreatedAt            time.Time
		LastUpdatedAt        *time.Time
		ArchivedAt           *time.Time
	}(GetRevokedGrantRow{})
	_ = struct {
		Scope    tenancy.Scope
		Subjects []string
	}(ListGrantsForSubjectsParams{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessTokenExpiresAt *time.Time
		RevocationReason     string
		CreatedAt            time.Time
		LastUpdatedAt        *time.Time
		ArchivedAt           *time.Time
	}(ListGrantsForSubjectsRow{})
	_ = struct {
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
		ID                   string
		Scope                tenancy.Scope
		ExpectedAccessToken  []byte
	}(RefreshGrantParams{})
	_ = struct {
		RevocationReason string
		AccessToken      []byte
		RefreshToken     []byte
		ID               string
		Scope            tenancy.Scope
	}(RevokeGrantParams{})
)
