// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package grantsdb

import (
	"context"
	"strings"
	"time"

	"github.com/primandproper/primitives-go/v2/tenancy"
)

const archiveGrantMySQL = `UPDATE {{prefix}}oauth2_grants SET
	archived_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = ?
	AND scope = ?`

const deleteGrantsForSubjectMySQL = `DELETE FROM {{prefix}}oauth2_grants
WHERE scope = ?
	AND subject = ?`

const getGrantMySQL = `SELECT
	{{prefix}}oauth2_grants.id,
	{{prefix}}oauth2_grants.scope,
	{{prefix}}oauth2_grants.subject,
	{{prefix}}oauth2_grants.provider,
	{{prefix}}oauth2_grants.provider_account_id,
	{{prefix}}oauth2_grants.granted_scopes,
	{{prefix}}oauth2_grants.access_token,
	{{prefix}}oauth2_grants.access_token_expires_at,
	{{prefix}}oauth2_grants.refresh_token,
	{{prefix}}oauth2_grants.revocation_reason,
	{{prefix}}oauth2_grants.created_at,
	{{prefix}}oauth2_grants.last_updated_at,
	{{prefix}}oauth2_grants.archived_at
FROM {{prefix}}oauth2_grants
WHERE {{prefix}}oauth2_grants.archived_at IS NULL
	AND {{prefix}}oauth2_grants.id = ?
	AND {{prefix}}oauth2_grants.scope = ?`

const getGrantForProviderMySQL = `SELECT
	{{prefix}}oauth2_grants.id,
	{{prefix}}oauth2_grants.scope,
	{{prefix}}oauth2_grants.subject,
	{{prefix}}oauth2_grants.provider,
	{{prefix}}oauth2_grants.provider_account_id,
	{{prefix}}oauth2_grants.granted_scopes,
	{{prefix}}oauth2_grants.access_token,
	{{prefix}}oauth2_grants.access_token_expires_at,
	{{prefix}}oauth2_grants.refresh_token,
	{{prefix}}oauth2_grants.revocation_reason,
	{{prefix}}oauth2_grants.created_at,
	{{prefix}}oauth2_grants.last_updated_at,
	{{prefix}}oauth2_grants.archived_at
FROM {{prefix}}oauth2_grants
WHERE {{prefix}}oauth2_grants.archived_at IS NULL
	AND {{prefix}}oauth2_grants.scope = ?
	AND {{prefix}}oauth2_grants.subject = ?
	AND {{prefix}}oauth2_grants.provider = ?`

const getRevokedGrantMySQL = `SELECT
	{{prefix}}oauth2_grants.id,
	{{prefix}}oauth2_grants.scope,
	{{prefix}}oauth2_grants.subject,
	{{prefix}}oauth2_grants.provider,
	{{prefix}}oauth2_grants.provider_account_id,
	{{prefix}}oauth2_grants.granted_scopes,
	{{prefix}}oauth2_grants.access_token,
	{{prefix}}oauth2_grants.access_token_expires_at,
	{{prefix}}oauth2_grants.refresh_token,
	{{prefix}}oauth2_grants.revocation_reason,
	{{prefix}}oauth2_grants.created_at,
	{{prefix}}oauth2_grants.last_updated_at,
	{{prefix}}oauth2_grants.archived_at
FROM {{prefix}}oauth2_grants
WHERE {{prefix}}oauth2_grants.id = ?
	AND {{prefix}}oauth2_grants.scope = ?
	AND {{prefix}}oauth2_grants.archived_at IS NOT NULL`

const listGrantsForSubjectsMySQL = `SELECT
	{{prefix}}oauth2_grants.id,
	{{prefix}}oauth2_grants.scope,
	{{prefix}}oauth2_grants.subject,
	{{prefix}}oauth2_grants.provider,
	{{prefix}}oauth2_grants.provider_account_id,
	{{prefix}}oauth2_grants.granted_scopes,
	{{prefix}}oauth2_grants.access_token_expires_at,
	{{prefix}}oauth2_grants.revocation_reason,
	{{prefix}}oauth2_grants.created_at,
	{{prefix}}oauth2_grants.last_updated_at,
	{{prefix}}oauth2_grants.archived_at
FROM {{prefix}}oauth2_grants
WHERE {{prefix}}oauth2_grants.scope = ?
	AND {{prefix}}oauth2_grants.subject IN (/*SLICE:subjects*/?)
ORDER BY {{prefix}}oauth2_grants.subject ASC, {{prefix}}oauth2_grants.created_at ASC`

const putGrantMySQL = `
INSERT INTO {{prefix}}oauth2_grants (
	id,
	scope,
	subject,
	provider,
	provider_account_id,
	granted_scopes,
	access_token,
	access_token_expires_at,
	refresh_token,
	revocation_reason
) VALUES (
	?,
	?,
	?,
	?,
	?,
	?,
	?,
	?,
	?,
	?
)
ON DUPLICATE KEY UPDATE
	id = VALUES(id),
	provider_account_id = VALUES(provider_account_id),
	granted_scopes = VALUES(granted_scopes),
	access_token = VALUES(access_token),
	access_token_expires_at = VALUES(access_token_expires_at),
	refresh_token = VALUES(refresh_token),
	revocation_reason = VALUES(revocation_reason),
	archived_at = NULL,
	last_updated_at = CURRENT_TIMESTAMP(6)`

const refreshGrantMySQL = `UPDATE {{prefix}}oauth2_grants SET
	access_token = ?,
	access_token_expires_at = ?,
	refresh_token = ?,
	last_updated_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = ?
	AND scope = ?
	AND access_token = ?`

const revokeGrantMySQL = `UPDATE {{prefix}}oauth2_grants SET
	revocation_reason = ?,
	access_token = ?,
	refresh_token = ?,
	last_updated_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = ?
	AND scope = ?`

// mysqlQueries answers every query in Querier against mysql.
type mysqlQueries struct {
	archiveGrant           string
	deleteGrantsForSubject string
	getGrant               string
	getGrantForProvider    string
	getRevokedGrant        string
	listGrantsForSubjects  string
	putGrant               string
	refreshGrant           string
	revokeGrant            string
}

// newMySQL returns the mysql querier with prefix substituted into every
// table name the analyzer identified.
func newMySQL(prefix string) *mysqlQueries {
	return &mysqlQueries{
		archiveGrant:           strings.ReplaceAll(archiveGrantMySQL, prefixMarker, prefix),
		deleteGrantsForSubject: strings.ReplaceAll(deleteGrantsForSubjectMySQL, prefixMarker, prefix),
		getGrant:               strings.ReplaceAll(getGrantMySQL, prefixMarker, prefix),
		getGrantForProvider:    strings.ReplaceAll(getGrantForProviderMySQL, prefixMarker, prefix),
		getRevokedGrant:        strings.ReplaceAll(getRevokedGrantMySQL, prefixMarker, prefix),
		listGrantsForSubjects:  strings.ReplaceAll(listGrantsForSubjectsMySQL, prefixMarker, prefix),
		putGrant:               strings.ReplaceAll(putGrantMySQL, prefixMarker, prefix),
		refreshGrant:           strings.ReplaceAll(refreshGrantMySQL, prefixMarker, prefix),
		revokeGrant:            strings.ReplaceAll(revokeGrantMySQL, prefixMarker, prefix),
	}
}

// ArchiveGrant runs the :execrows query against mysql.
func (q *mysqlQueries) ArchiveGrant(ctx context.Context, db DBTX, arg ArchiveGrantParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.archiveGrant,
		arg.ID,
		arg.Scope,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// DeleteGrantsForSubject runs the :execrows query against mysql.
func (q *mysqlQueries) DeleteGrantsForSubject(ctx context.Context, db DBTX, arg DeleteGrantsForSubjectParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.deleteGrantsForSubject,
		arg.Scope,
		arg.Subject,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// GetGrant runs the :one query against mysql.
func (q *mysqlQueries) GetGrant(ctx context.Context, db DBTX, arg GetGrantParams) (GetGrantRow, error) {
	row := db.QueryRowContext(ctx, q.getGrant,
		arg.ID,
		arg.Scope,
	)

	var i GetGrantRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.Subject,
		&i.Provider,
		&i.ProviderAccountID,
		&i.GrantedScopes,
		&i.AccessToken,
		&i.AccessTokenExpiresAt,
		&i.RefreshToken,
		&i.RevocationReason,
		&i.CreatedAt,
		&i.LastUpdatedAt,
		&i.ArchivedAt,
	)

	return i, err
}

// GetGrantForProvider runs the :one query against mysql.
func (q *mysqlQueries) GetGrantForProvider(ctx context.Context, db DBTX, arg GetGrantForProviderParams) (GetGrantForProviderRow, error) {
	row := db.QueryRowContext(ctx, q.getGrantForProvider,
		arg.Scope,
		arg.Subject,
		arg.Provider,
	)

	var i GetGrantForProviderRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.Subject,
		&i.Provider,
		&i.ProviderAccountID,
		&i.GrantedScopes,
		&i.AccessToken,
		&i.AccessTokenExpiresAt,
		&i.RefreshToken,
		&i.RevocationReason,
		&i.CreatedAt,
		&i.LastUpdatedAt,
		&i.ArchivedAt,
	)

	return i, err
}

// GetRevokedGrant runs the :one query against mysql.
func (q *mysqlQueries) GetRevokedGrant(ctx context.Context, db DBTX, arg GetRevokedGrantParams) (GetRevokedGrantRow, error) {
	row := db.QueryRowContext(ctx, q.getRevokedGrant,
		arg.ID,
		arg.Scope,
	)

	var i GetRevokedGrantRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.Subject,
		&i.Provider,
		&i.ProviderAccountID,
		&i.GrantedScopes,
		&i.AccessToken,
		&i.AccessTokenExpiresAt,
		&i.RefreshToken,
		&i.RevocationReason,
		&i.CreatedAt,
		&i.LastUpdatedAt,
		&i.ArchivedAt,
	)

	return i, err
}

// ListGrantsForSubjects runs the :many query against mysql.
func (q *mysqlQueries) ListGrantsForSubjects(ctx context.Context, db DBTX, arg ListGrantsForSubjectsParams) ([]ListGrantsForSubjectsRow, error) {
	query := q.listGrantsForSubjects

	args := make([]any, 0, 1+len(arg.Subjects))

	args = append(args, arg.Scope)

	query = strings.Replace(query, "/*SLICE:subjects*/?", slicePlaceholders("?", len(arg.Subjects)), 1)

	for _, v := range arg.Subjects {
		args = append(args, v)
	}

	rows, err := db.QueryContext(ctx, query, args...)
	if err != nil {
		return nil, err
	}

	defer func() { _ = rows.Close() }()

	var items []ListGrantsForSubjectsRow

	for rows.Next() {
		var i ListGrantsForSubjectsRow

		if err := rows.Scan(
			&i.ID,
			&i.Scope,
			&i.Subject,
			&i.Provider,
			&i.ProviderAccountID,
			&i.GrantedScopes,
			&i.AccessTokenExpiresAt,
			&i.RevocationReason,
			&i.CreatedAt,
			&i.LastUpdatedAt,
			&i.ArchivedAt,
		); err != nil {
			return nil, err
		}

		items = append(items, i)
	}

	if err := rows.Err(); err != nil {
		return nil, err
	}

	return items, nil
}

// PutGrant runs the :exec query against mysql.
func (q *mysqlQueries) PutGrant(ctx context.Context, db DBTX, arg PutGrantParams) error {
	_, err := db.ExecContext(ctx, q.putGrant,
		arg.ID,
		arg.Scope,
		arg.Subject,
		arg.Provider,
		arg.ProviderAccountID,
		arg.GrantedScopes,
		arg.AccessToken,
		arg.AccessTokenExpiresAt,
		arg.RefreshToken,
		arg.RevocationReason,
	)

	return err
}

// RefreshGrant runs the :execrows query against mysql.
func (q *mysqlQueries) RefreshGrant(ctx context.Context, db DBTX, arg RefreshGrantParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.refreshGrant,
		arg.AccessToken,
		arg.AccessTokenExpiresAt,
		arg.RefreshToken,
		arg.ID,
		arg.Scope,
		arg.ExpectedAccessToken,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// RevokeGrant runs the :execrows query against mysql.
func (q *mysqlQueries) RevokeGrant(ctx context.Context, db DBTX, arg RevokeGrantParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.revokeGrant,
		arg.RevocationReason,
		arg.AccessToken,
		arg.RefreshToken,
		arg.ID,
		arg.Scope,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// Shape assertions.
//
// Each conversion below compiles only if the shared type still has exactly
// these fields, with these types, in this order — which is what makes a
// disagreement between dialects a compile error here rather than a
// transposition at run time.
var (
	_ = struct {
		ID    string
		Scope tenancy.Scope
	}(ArchiveGrantParams{})
	_ = struct {
		Scope   tenancy.Scope
		Subject string
	}(DeleteGrantsForSubjectParams{})
	_ = struct {
		ID    string
		Scope tenancy.Scope
	}(GetGrantParams{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
		RevocationReason     string
		CreatedAt            time.Time
		LastUpdatedAt        *time.Time
		ArchivedAt           *time.Time
	}(GetGrantRow{})
	_ = struct {
		Scope    tenancy.Scope
		Subject  string
		Provider string
	}(GetGrantForProviderParams{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
		RevocationReason     string
		CreatedAt            time.Time
		LastUpdatedAt        *time.Time
		ArchivedAt           *time.Time
	}(GetGrantForProviderRow{})
	_ = struct {
		ID    string
		Scope tenancy.Scope
	}(GetRevokedGrantParams{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
		RevocationReason     string
		CreatedAt            time.Time
		LastUpdatedAt        *time.Time
		ArchivedAt           *time.Time
	}(GetRevokedGrantRow{})
	_ = struct {
		Scope    tenancy.Scope
		Subjects []string
	}(ListGrantsForSubjectsParams{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessTokenExpiresAt *time.Time
		RevocationReason     string
		CreatedAt            time.Time
		LastUpdatedAt        *time.Time
		ArchivedAt           *time.Time
	}(ListGrantsForSubjectsRow{})
	_ = struct {
		ID                   string
		Scope                tenancy.Scope
		Subject              string
		Provider             string
		ProviderAccountID    string
		GrantedScopes        string
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
		RevocationReason     string
	}(PutGrantParams{})
	_ = struct {
		AccessToken          []byte
		AccessTokenExpiresAt *time.Time
		RefreshToken         []byte
		ID                   string
		Scope                tenancy.Scope
		ExpectedAccessToken  []byte
	}(RefreshGrantParams{})
	_ = struct {
		RevocationReason string
		AccessToken      []byte
		RefreshToken     []byte
		ID               string
		Scope            tenancy.Scope
	}(RevokeGrantParams{})
)
