sqlc_version: 1.31.1
package: signindb
out: internal/signindb

# The keys of `schemas:` are the dialect roster. The schema files live beside
# their source: rendered from authentication/signin/refreshtokens/migrations by
# `go run ./internal/queriesgen -schema <dialect>`, at the empty table prefix —
# never edited by hand.
schemas:
  postgresql: migrations/schema/postgres.sql
  mysql: migrations/schema/mysql.sql
  sqlite: migrations/schema/sqlite.sql

queries:
  postgresql: internal/queries/postgres_generated.sql
  mysql: internal/queries/mysql_generated.sql
  sqlite: internal/queries/sqlite_generated.sql

options:
  # ddl.Qualify prefixes the table name from consumer config at construction;
  # the generated SQL carries {{prefix}} markers substituted once by New.
  table_prefix_var: true

  # MySQL accepts only a bare placeholder in LIMIT and names it `limit`;
  # Postgres rejects `limit` as an argument name. The name converges here, for
  # the one bounded statement in this corpus: the listing of a person's live
  # logins.
  rename_params:
    mysql:
      limit: result_limit

  type_overrides:
    # The scope is bound as the Scope itself rather than as a string derived
    # from it, so an unset scope is a driver error instead of a wider result
    # set. See the tenancy package.
    - {column: "*.scope", go_type: github.com/primandproper/primitives-go/v2/tenancy.Scope}
    # Neither SQLite nor Postgres resolves a type for a LIMIT's argument.
    - {column: "*.result_limit", go_type: int64}
