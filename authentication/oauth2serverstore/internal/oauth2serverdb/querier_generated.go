// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package oauth2serverdb

import (
	"context"
)

// Querier is every query in this package, with one signature per query
// regardless of which dialect answers it.
//
// That is the point of this package: the params type, the row type, and the
// method signature are the same on every dialect, so nothing above this line
// knows or cares which database is underneath.
//
// # A note on the :execrows count
//
// MySQL reports rows *changed*: an UPDATE that sets a column to the value it
// already held affects zero rows there. Postgres and SQLite report rows
// *matched*, and count that same UPDATE as one.
//
// So a statement that gates on the count — treating zero as "not found" — is
// correct on two engines and wrong on the third. Either give it a predicate
// that discriminates, or set clientFoundRows=true in the MySQL DSN, which
// switches MySQL to matched semantics.
type Querier interface {
	// ConsumeAuthorizationCode runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ConsumeAuthorizationCode(ctx context.Context, db DBTX, arg ConsumeAuthorizationCodeParams) (int64, error)
	// ConsumeRefreshToken runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ConsumeRefreshToken(ctx context.Context, db DBTX, arg ConsumeRefreshTokenParams) (int64, error)
	// CreateAccessToken runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	CreateAccessToken(ctx context.Context, db DBTX, arg CreateAccessTokenParams) (int64, error)
	// CreateAuthorizationCode runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	CreateAuthorizationCode(ctx context.Context, db DBTX, arg CreateAuthorizationCodeParams) (int64, error)
	// CreateClient runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	CreateClient(ctx context.Context, db DBTX, arg CreateClientParams) (int64, error)
	// CreateRefreshToken runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	CreateRefreshToken(ctx context.Context, db DBTX, arg CreateRefreshTokenParams) (int64, error)
	// DeleteClient runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	DeleteClient(ctx context.Context, db DBTX, arg DeleteClientParams) (int64, error)
	// GetAccessToken runs the :one query.
	GetAccessToken(ctx context.Context, db DBTX, arg GetAccessTokenParams) (GetAccessTokenRow, error)
	// GetAuthorizationCode runs the :one query.
	GetAuthorizationCode(ctx context.Context, db DBTX, arg GetAuthorizationCodeParams) (GetAuthorizationCodeRow, error)
	// GetClient runs the :one query.
	GetClient(ctx context.Context, db DBTX, arg GetClientParams) (GetClientRow, error)
	// GetRefreshToken runs the :one query.
	GetRefreshToken(ctx context.Context, db DBTX, arg GetRefreshTokenParams) (GetRefreshTokenRow, error)
	// RevokeAccessToken runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RevokeAccessToken(ctx context.Context, db DBTX, arg RevokeAccessTokenParams) (int64, error)
	// RevokeAccessTokenFamily runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RevokeAccessTokenFamily(ctx context.Context, db DBTX, arg RevokeAccessTokenFamilyParams) (int64, error)
	// RevokeRefreshToken runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RevokeRefreshToken(ctx context.Context, db DBTX, arg RevokeRefreshTokenParams) (int64, error)
	// RevokeRefreshTokenFamily runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RevokeRefreshTokenFamily(ctx context.Context, db DBTX, arg RevokeRefreshTokenFamilyParams) (int64, error)
	// SweepAccessTokens runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	SweepAccessTokens(ctx context.Context, db DBTX, arg SweepAccessTokensParams) (int64, error)
	// SweepAuthorizationCodes runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	SweepAuthorizationCodes(ctx context.Context, db DBTX, arg SweepAuthorizationCodesParams) (int64, error)
	// SweepClients runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	SweepClients(ctx context.Context, db DBTX, arg SweepClientsParams) (int64, error)
	// SweepRefreshTokens runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	SweepRefreshTokens(ctx context.Context, db DBTX, arg SweepRefreshTokensParams) (int64, error)
}
