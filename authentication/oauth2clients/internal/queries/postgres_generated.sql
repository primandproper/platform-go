-- Code generated by github.com/primandproper/platform-go/v14/authentication/oauth2clients/internal/queriesgen. DO NOT EDIT.

-- name: GetRegisteredClient :one
SELECT
	oauth2_registered_clients.id,
	oauth2_registered_clients.scope,
	oauth2_registered_clients.belongs_to_user,
	oauth2_registered_clients.name,
	oauth2_registered_clients.description,
	oauth2_registered_clients.client_id,
	oauth2_registered_clients.secret_hash,
	oauth2_registered_clients.redirect_uris,
	oauth2_registered_clients.scopes,
	oauth2_registered_clients.created_at,
	oauth2_registered_clients.last_updated_at,
	oauth2_registered_clients.archived_at
FROM oauth2_registered_clients
WHERE oauth2_registered_clients.archived_at IS NULL
	AND oauth2_registered_clients.id = sqlc.arg(id)
	AND oauth2_registered_clients.scope = sqlc.arg(scope);

-- name: ListRegisteredClients :many
SELECT
	oauth2_registered_clients.id,
	oauth2_registered_clients.scope,
	oauth2_registered_clients.belongs_to_user,
	oauth2_registered_clients.name,
	oauth2_registered_clients.description,
	oauth2_registered_clients.client_id,
	oauth2_registered_clients.secret_hash,
	oauth2_registered_clients.redirect_uris,
	oauth2_registered_clients.scopes,
	oauth2_registered_clients.created_at,
	oauth2_registered_clients.last_updated_at,
	oauth2_registered_clients.archived_at,
	(
		SELECT COUNT(oauth2_registered_clients.id)
		FROM oauth2_registered_clients
		WHERE oauth2_registered_clients.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND oauth2_registered_clients.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				oauth2_registered_clients.last_updated_at IS NULL
				OR oauth2_registered_clients.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				oauth2_registered_clients.last_updated_at IS NULL
				OR oauth2_registered_clients.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
			AND oauth2_registered_clients.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(oauth2_registered_clients.id)
		FROM oauth2_registered_clients
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
			AND oauth2_registered_clients.scope = sqlc.arg(scope)
	) AS total_count
FROM oauth2_registered_clients
WHERE oauth2_registered_clients.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND oauth2_registered_clients.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		oauth2_registered_clients.last_updated_at IS NULL
		OR oauth2_registered_clients.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		oauth2_registered_clients.last_updated_at IS NULL
		OR oauth2_registered_clients.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
	AND oauth2_registered_clients.scope = sqlc.arg(scope)
	AND oauth2_registered_clients.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY oauth2_registered_clients.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListRegisteredClientsDescending :many
SELECT
	oauth2_registered_clients.id,
	oauth2_registered_clients.scope,
	oauth2_registered_clients.belongs_to_user,
	oauth2_registered_clients.name,
	oauth2_registered_clients.description,
	oauth2_registered_clients.client_id,
	oauth2_registered_clients.secret_hash,
	oauth2_registered_clients.redirect_uris,
	oauth2_registered_clients.scopes,
	oauth2_registered_clients.created_at,
	oauth2_registered_clients.last_updated_at,
	oauth2_registered_clients.archived_at,
	(
		SELECT COUNT(oauth2_registered_clients.id)
		FROM oauth2_registered_clients
		WHERE oauth2_registered_clients.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND oauth2_registered_clients.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				oauth2_registered_clients.last_updated_at IS NULL
				OR oauth2_registered_clients.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				oauth2_registered_clients.last_updated_at IS NULL
				OR oauth2_registered_clients.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
			AND oauth2_registered_clients.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(oauth2_registered_clients.id)
		FROM oauth2_registered_clients
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
			AND oauth2_registered_clients.scope = sqlc.arg(scope)
	) AS total_count
FROM oauth2_registered_clients
WHERE oauth2_registered_clients.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND oauth2_registered_clients.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		oauth2_registered_clients.last_updated_at IS NULL
		OR oauth2_registered_clients.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		oauth2_registered_clients.last_updated_at IS NULL
		OR oauth2_registered_clients.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
	AND oauth2_registered_clients.scope = sqlc.arg(scope)
	AND (oauth2_registered_clients.id <= COALESCE(sqlc.narg(page_cursor), oauth2_registered_clients.id) AND oauth2_registered_clients.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY oauth2_registered_clients.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: UpdateRegisteredClient :execrows
UPDATE oauth2_registered_clients SET
	name = sqlc.arg(name),
	description = sqlc.arg(description),
	redirect_uris = sqlc.arg(redirect_uris),
	scopes = sqlc.arg(scopes),
	last_updated_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: ArchiveRegisteredClient :execrows
UPDATE oauth2_registered_clients SET
	archived_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: CreateRegisteredClient :execrows
INSERT INTO oauth2_registered_clients (
	id,
	scope,
	belongs_to_user,
	name,
	description,
	client_id,
	secret_hash,
	redirect_uris,
	scopes
) VALUES (
	sqlc.arg(id),
	sqlc.arg(scope),
	sqlc.arg(belongs_to_user),
	sqlc.arg(name),
	sqlc.arg(description),
	sqlc.arg(client_id),
	sqlc.arg(secret_hash),
	sqlc.arg(redirect_uris),
	sqlc.arg(scopes)
)
ON CONFLICT (client_id) DO NOTHING;

-- name: GetArchivedRegisteredClient :one
SELECT
	oauth2_registered_clients.id,
	oauth2_registered_clients.scope,
	oauth2_registered_clients.belongs_to_user,
	oauth2_registered_clients.name,
	oauth2_registered_clients.description,
	oauth2_registered_clients.client_id,
	oauth2_registered_clients.secret_hash,
	oauth2_registered_clients.redirect_uris,
	oauth2_registered_clients.scopes,
	oauth2_registered_clients.created_at,
	oauth2_registered_clients.last_updated_at,
	oauth2_registered_clients.archived_at
FROM oauth2_registered_clients
WHERE oauth2_registered_clients.id = sqlc.arg(id)
	AND oauth2_registered_clients.scope = sqlc.arg(scope)
	AND oauth2_registered_clients.archived_at IS NOT NULL;

-- name: GetRegisteredClientByClientID :one
SELECT
	oauth2_registered_clients.id,
	oauth2_registered_clients.scope,
	oauth2_registered_clients.belongs_to_user,
	oauth2_registered_clients.name,
	oauth2_registered_clients.description,
	oauth2_registered_clients.client_id,
	oauth2_registered_clients.secret_hash,
	oauth2_registered_clients.redirect_uris,
	oauth2_registered_clients.scopes,
	oauth2_registered_clients.created_at,
	oauth2_registered_clients.last_updated_at,
	oauth2_registered_clients.archived_at
FROM oauth2_registered_clients
WHERE oauth2_registered_clients.client_id = sqlc.arg(client_id);

-- name: ListRegisteredClientsForOwner :many
SELECT
	oauth2_registered_clients.id,
	oauth2_registered_clients.scope,
	oauth2_registered_clients.belongs_to_user,
	oauth2_registered_clients.name,
	oauth2_registered_clients.description,
	oauth2_registered_clients.client_id,
	oauth2_registered_clients.secret_hash,
	oauth2_registered_clients.redirect_uris,
	oauth2_registered_clients.scopes,
	oauth2_registered_clients.created_at,
	oauth2_registered_clients.last_updated_at,
	oauth2_registered_clients.archived_at,
	(
		SELECT COUNT(oauth2_registered_clients.id)
		FROM oauth2_registered_clients
		WHERE oauth2_registered_clients.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND oauth2_registered_clients.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				oauth2_registered_clients.last_updated_at IS NULL
				OR oauth2_registered_clients.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				oauth2_registered_clients.last_updated_at IS NULL
				OR oauth2_registered_clients.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
			AND oauth2_registered_clients.scope = sqlc.arg(scope)
			AND oauth2_registered_clients.belongs_to_user = sqlc.arg(belongs_to_user)
	) AS filtered_count,
	(
		SELECT COUNT(oauth2_registered_clients.id)
		FROM oauth2_registered_clients
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
			AND oauth2_registered_clients.scope = sqlc.arg(scope)
			AND oauth2_registered_clients.belongs_to_user = sqlc.arg(belongs_to_user)
	) AS total_count
FROM oauth2_registered_clients
WHERE oauth2_registered_clients.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND oauth2_registered_clients.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		oauth2_registered_clients.last_updated_at IS NULL
		OR oauth2_registered_clients.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		oauth2_registered_clients.last_updated_at IS NULL
		OR oauth2_registered_clients.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
	AND oauth2_registered_clients.scope = sqlc.arg(scope)
	AND oauth2_registered_clients.belongs_to_user = sqlc.arg(belongs_to_user)
	AND oauth2_registered_clients.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY oauth2_registered_clients.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListRegisteredClientsForOwnerDescending :many
SELECT
	oauth2_registered_clients.id,
	oauth2_registered_clients.scope,
	oauth2_registered_clients.belongs_to_user,
	oauth2_registered_clients.name,
	oauth2_registered_clients.description,
	oauth2_registered_clients.client_id,
	oauth2_registered_clients.secret_hash,
	oauth2_registered_clients.redirect_uris,
	oauth2_registered_clients.scopes,
	oauth2_registered_clients.created_at,
	oauth2_registered_clients.last_updated_at,
	oauth2_registered_clients.archived_at,
	(
		SELECT COUNT(oauth2_registered_clients.id)
		FROM oauth2_registered_clients
		WHERE oauth2_registered_clients.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND oauth2_registered_clients.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				oauth2_registered_clients.last_updated_at IS NULL
				OR oauth2_registered_clients.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				oauth2_registered_clients.last_updated_at IS NULL
				OR oauth2_registered_clients.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
			AND oauth2_registered_clients.scope = sqlc.arg(scope)
			AND oauth2_registered_clients.belongs_to_user = sqlc.arg(belongs_to_user)
	) AS filtered_count,
	(
		SELECT COUNT(oauth2_registered_clients.id)
		FROM oauth2_registered_clients
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
			AND oauth2_registered_clients.scope = sqlc.arg(scope)
			AND oauth2_registered_clients.belongs_to_user = sqlc.arg(belongs_to_user)
	) AS total_count
FROM oauth2_registered_clients
WHERE oauth2_registered_clients.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND oauth2_registered_clients.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		oauth2_registered_clients.last_updated_at IS NULL
		OR oauth2_registered_clients.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		oauth2_registered_clients.last_updated_at IS NULL
		OR oauth2_registered_clients.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR oauth2_registered_clients.archived_at IS NULL)
	AND oauth2_registered_clients.scope = sqlc.arg(scope)
	AND oauth2_registered_clients.belongs_to_user = sqlc.arg(belongs_to_user)
	AND (oauth2_registered_clients.id <= COALESCE(sqlc.narg(page_cursor), oauth2_registered_clients.id) AND oauth2_registered_clients.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY oauth2_registered_clients.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);
