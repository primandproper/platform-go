// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package passkeysdb

import (
	"context"
	"strings"
	"time"

	"github.com/primandproper/primitives-go/v2/tenancy"
)

const archiveCredentialForUserSQLite = `UPDATE {{prefix}}webauthn_credentials SET
	archived_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = ?1
	AND scope = ?2
	AND belongs_to_user = ?3`

const createCredentialSQLite = `
INSERT INTO {{prefix}}webauthn_credentials (
	id,
	scope,
	belongs_to_user,
	credential_id,
	public_key,
	transports,
	friendly_name,
	sign_count
) VALUES (
	?1,
	?2,
	?3,
	?4,
	?5,
	?6,
	?7,
	?8
)`

const deleteCredentialsForUserSQLite = `DELETE FROM {{prefix}}webauthn_credentials
WHERE scope = ?1
	AND belongs_to_user = ?2`

const getArchivedCredentialSQLite = `SELECT
	{{prefix}}webauthn_credentials.id,
	{{prefix}}webauthn_credentials.scope,
	{{prefix}}webauthn_credentials.belongs_to_user,
	{{prefix}}webauthn_credentials.credential_id,
	{{prefix}}webauthn_credentials.public_key,
	{{prefix}}webauthn_credentials.transports,
	{{prefix}}webauthn_credentials.friendly_name,
	{{prefix}}webauthn_credentials.sign_count,
	{{prefix}}webauthn_credentials.created_at,
	{{prefix}}webauthn_credentials.last_updated_at,
	{{prefix}}webauthn_credentials.last_used_at,
	{{prefix}}webauthn_credentials.archived_at
FROM {{prefix}}webauthn_credentials
WHERE {{prefix}}webauthn_credentials.id = ?1
	AND {{prefix}}webauthn_credentials.scope = ?2
	AND {{prefix}}webauthn_credentials.archived_at IS NOT NULL`

const getCredentialSQLite = `SELECT
	{{prefix}}webauthn_credentials.id,
	{{prefix}}webauthn_credentials.scope,
	{{prefix}}webauthn_credentials.belongs_to_user,
	{{prefix}}webauthn_credentials.credential_id,
	{{prefix}}webauthn_credentials.public_key,
	{{prefix}}webauthn_credentials.transports,
	{{prefix}}webauthn_credentials.friendly_name,
	{{prefix}}webauthn_credentials.sign_count,
	{{prefix}}webauthn_credentials.created_at,
	{{prefix}}webauthn_credentials.last_updated_at,
	{{prefix}}webauthn_credentials.last_used_at,
	{{prefix}}webauthn_credentials.archived_at
FROM {{prefix}}webauthn_credentials
WHERE {{prefix}}webauthn_credentials.archived_at IS NULL
	AND {{prefix}}webauthn_credentials.id = ?1
	AND {{prefix}}webauthn_credentials.scope = ?2`

const getCredentialByCredentialIDSQLite = `SELECT
	{{prefix}}webauthn_credentials.id,
	{{prefix}}webauthn_credentials.scope,
	{{prefix}}webauthn_credentials.belongs_to_user,
	{{prefix}}webauthn_credentials.credential_id,
	{{prefix}}webauthn_credentials.public_key,
	{{prefix}}webauthn_credentials.transports,
	{{prefix}}webauthn_credentials.friendly_name,
	{{prefix}}webauthn_credentials.sign_count,
	{{prefix}}webauthn_credentials.created_at,
	{{prefix}}webauthn_credentials.last_updated_at,
	{{prefix}}webauthn_credentials.last_used_at,
	{{prefix}}webauthn_credentials.archived_at
FROM {{prefix}}webauthn_credentials
WHERE {{prefix}}webauthn_credentials.archived_at IS NULL
	AND {{prefix}}webauthn_credentials.credential_id = ?1
	AND {{prefix}}webauthn_credentials.scope = ?2`

const listCredentialsForUserSQLite = `SELECT
	{{prefix}}webauthn_credentials.id,
	{{prefix}}webauthn_credentials.scope,
	{{prefix}}webauthn_credentials.belongs_to_user,
	{{prefix}}webauthn_credentials.credential_id,
	{{prefix}}webauthn_credentials.public_key,
	{{prefix}}webauthn_credentials.transports,
	{{prefix}}webauthn_credentials.friendly_name,
	{{prefix}}webauthn_credentials.sign_count,
	{{prefix}}webauthn_credentials.created_at,
	{{prefix}}webauthn_credentials.last_updated_at,
	{{prefix}}webauthn_credentials.last_used_at,
	{{prefix}}webauthn_credentials.archived_at
FROM {{prefix}}webauthn_credentials
WHERE {{prefix}}webauthn_credentials.archived_at IS NULL
	AND {{prefix}}webauthn_credentials.scope = ?1
	AND {{prefix}}webauthn_credentials.belongs_to_user = ?2
ORDER BY {{prefix}}webauthn_credentials.created_at ASC, {{prefix}}webauthn_credentials.id ASC`

const listCredentialsForUsersSQLite = `SELECT
	{{prefix}}webauthn_credentials.id,
	{{prefix}}webauthn_credentials.scope,
	{{prefix}}webauthn_credentials.belongs_to_user,
	{{prefix}}webauthn_credentials.credential_id,
	{{prefix}}webauthn_credentials.public_key,
	{{prefix}}webauthn_credentials.transports,
	{{prefix}}webauthn_credentials.friendly_name,
	{{prefix}}webauthn_credentials.sign_count,
	{{prefix}}webauthn_credentials.created_at,
	{{prefix}}webauthn_credentials.last_updated_at,
	{{prefix}}webauthn_credentials.last_used_at,
	{{prefix}}webauthn_credentials.archived_at
FROM {{prefix}}webauthn_credentials
WHERE {{prefix}}webauthn_credentials.scope = ?1
	AND {{prefix}}webauthn_credentials.belongs_to_user IN (/*SLICE:users*/?)
ORDER BY {{prefix}}webauthn_credentials.belongs_to_user ASC, {{prefix}}webauthn_credentials.created_at ASC`

const recordCredentialUseSQLite = `UPDATE {{prefix}}webauthn_credentials SET
	sign_count = ?1,
	last_used_at = ?2,
	last_updated_at = CURRENT_TIMESTAMP
WHERE archived_at IS NULL
	AND id = ?3
	AND scope = ?4`

// sqliteQueries answers every query in Querier against sqlite.
type sqliteQueries struct {
	archiveCredentialForUser    string
	createCredential            string
	deleteCredentialsForUser    string
	getArchivedCredential       string
	getCredential               string
	getCredentialByCredentialID string
	listCredentialsForUser      string
	listCredentialsForUsers     string
	recordCredentialUse         string
}

// newSQLite returns the sqlite querier with prefix substituted into every
// table name the analyzer identified.
func newSQLite(prefix string) *sqliteQueries {
	return &sqliteQueries{
		archiveCredentialForUser:    strings.ReplaceAll(archiveCredentialForUserSQLite, prefixMarker, prefix),
		createCredential:            strings.ReplaceAll(createCredentialSQLite, prefixMarker, prefix),
		deleteCredentialsForUser:    strings.ReplaceAll(deleteCredentialsForUserSQLite, prefixMarker, prefix),
		getArchivedCredential:       strings.ReplaceAll(getArchivedCredentialSQLite, prefixMarker, prefix),
		getCredential:               strings.ReplaceAll(getCredentialSQLite, prefixMarker, prefix),
		getCredentialByCredentialID: strings.ReplaceAll(getCredentialByCredentialIDSQLite, prefixMarker, prefix),
		listCredentialsForUser:      strings.ReplaceAll(listCredentialsForUserSQLite, prefixMarker, prefix),
		listCredentialsForUsers:     strings.ReplaceAll(listCredentialsForUsersSQLite, prefixMarker, prefix),
		recordCredentialUse:         strings.ReplaceAll(recordCredentialUseSQLite, prefixMarker, prefix),
	}
}

// timeText renders a time argument as the text this engine stores timestamps as.
//
// This engine has no date type: a DATETIME column holds text, and a comparison
// between two of them compares two strings. What a driver writes for a bound
// time.Time is Go's own rendering of it, whose leading characters happen to be
// the stored shape — but only while the value is UTC. A time carrying any other
// zone puts its own wall clock in those leading characters, so every window
// comparison is off by that offset, silently, and only for the callers whose
// clock is not UTC.
//
// Converting here is what makes the bound text the stored shape by
// construction rather than by accident, whatever zone the caller's time
// carries. The layout is whole seconds, which is what CURRENT_TIMESTAMP writes,
// so a bound value and a stored one are one shape rather than two that sort
// alike — which does mean a sub-second time bound here is stored truncated to
// the second on this engine.
func timeText(t time.Time) string {
	return t.UTC().Format("2006-01-02 15:04:05")
}

// timeTextPtr is timeText for a nullable argument. It preserves nil, so a
// field the caller left unset still binds NULL rather than the zero time.
func timeTextPtr(t *time.Time) any {
	if t == nil {
		return nil
	}

	return timeText(*t)
}

// ArchiveCredentialForUser runs the :execrows query against sqlite.
func (q *sqliteQueries) ArchiveCredentialForUser(ctx context.Context, db DBTX, arg ArchiveCredentialForUserParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.archiveCredentialForUser,
		arg.ID,
		arg.Scope,
		arg.BelongsToUser,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// CreateCredential runs the :exec query against sqlite.
func (q *sqliteQueries) CreateCredential(ctx context.Context, db DBTX, arg CreateCredentialParams) error {
	_, err := db.ExecContext(ctx, q.createCredential,
		arg.ID,
		arg.Scope,
		arg.BelongsToUser,
		arg.CredentialID,
		arg.PublicKey,
		arg.Transports,
		arg.FriendlyName,
		arg.SignCount,
	)

	return err
}

// DeleteCredentialsForUser runs the :execrows query against sqlite.
func (q *sqliteQueries) DeleteCredentialsForUser(ctx context.Context, db DBTX, arg DeleteCredentialsForUserParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.deleteCredentialsForUser,
		arg.Scope,
		arg.BelongsToUser,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// GetArchivedCredential runs the :one query against sqlite.
func (q *sqliteQueries) GetArchivedCredential(ctx context.Context, db DBTX, arg GetArchivedCredentialParams) (GetArchivedCredentialRow, error) {
	row := db.QueryRowContext(ctx, q.getArchivedCredential,
		arg.ID,
		arg.Scope,
	)

	var i GetArchivedCredentialRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.BelongsToUser,
		&i.CredentialID,
		&i.PublicKey,
		&i.Transports,
		&i.FriendlyName,
		&i.SignCount,
		&i.CreatedAt,
		&i.LastUpdatedAt,
		&i.LastUsedAt,
		&i.ArchivedAt,
	)

	return i, err
}

// GetCredential runs the :one query against sqlite.
func (q *sqliteQueries) GetCredential(ctx context.Context, db DBTX, arg GetCredentialParams) (GetCredentialRow, error) {
	row := db.QueryRowContext(ctx, q.getCredential,
		arg.ID,
		arg.Scope,
	)

	var i GetCredentialRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.BelongsToUser,
		&i.CredentialID,
		&i.PublicKey,
		&i.Transports,
		&i.FriendlyName,
		&i.SignCount,
		&i.CreatedAt,
		&i.LastUpdatedAt,
		&i.LastUsedAt,
		&i.ArchivedAt,
	)

	return i, err
}

// GetCredentialByCredentialID runs the :one query against sqlite.
func (q *sqliteQueries) GetCredentialByCredentialID(ctx context.Context, db DBTX, arg GetCredentialByCredentialIDParams) (GetCredentialByCredentialIDRow, error) {
	row := db.QueryRowContext(ctx, q.getCredentialByCredentialID,
		arg.CredentialID,
		arg.Scope,
	)

	var i GetCredentialByCredentialIDRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.BelongsToUser,
		&i.CredentialID,
		&i.PublicKey,
		&i.Transports,
		&i.FriendlyName,
		&i.SignCount,
		&i.CreatedAt,
		&i.LastUpdatedAt,
		&i.LastUsedAt,
		&i.ArchivedAt,
	)

	return i, err
}

// ListCredentialsForUser runs the :many query against sqlite.
func (q *sqliteQueries) ListCredentialsForUser(ctx context.Context, db DBTX, arg ListCredentialsForUserParams) ([]ListCredentialsForUserRow, error) {
	rows, err := db.QueryContext(ctx, q.listCredentialsForUser,
		arg.Scope,
		arg.BelongsToUser,
	)
	if err != nil {
		return nil, err
	}

	defer func() { _ = rows.Close() }()

	var items []ListCredentialsForUserRow

	for rows.Next() {
		var i ListCredentialsForUserRow

		if err := rows.Scan(
			&i.ID,
			&i.Scope,
			&i.BelongsToUser,
			&i.CredentialID,
			&i.PublicKey,
			&i.Transports,
			&i.FriendlyName,
			&i.SignCount,
			&i.CreatedAt,
			&i.LastUpdatedAt,
			&i.LastUsedAt,
			&i.ArchivedAt,
		); err != nil {
			return nil, err
		}

		items = append(items, i)
	}

	if err := rows.Err(); err != nil {
		return nil, err
	}

	return items, nil
}

// ListCredentialsForUsers runs the :many query against sqlite.
func (q *sqliteQueries) ListCredentialsForUsers(ctx context.Context, db DBTX, arg ListCredentialsForUsersParams) ([]ListCredentialsForUsersRow, error) {
	query := q.listCredentialsForUsers

	args := make([]any, 0, 1+len(arg.Users))

	args = append(args, arg.Scope)

	query = strings.Replace(query, "/*SLICE:users*/?", slicePlaceholders("?", len(arg.Users)), 1)

	for _, v := range arg.Users {
		args = append(args, v)
	}

	rows, err := db.QueryContext(ctx, query, args...)
	if err != nil {
		return nil, err
	}

	defer func() { _ = rows.Close() }()

	var items []ListCredentialsForUsersRow

	for rows.Next() {
		var i ListCredentialsForUsersRow

		if err := rows.Scan(
			&i.ID,
			&i.Scope,
			&i.BelongsToUser,
			&i.CredentialID,
			&i.PublicKey,
			&i.Transports,
			&i.FriendlyName,
			&i.SignCount,
			&i.CreatedAt,
			&i.LastUpdatedAt,
			&i.LastUsedAt,
			&i.ArchivedAt,
		); err != nil {
			return nil, err
		}

		items = append(items, i)
	}

	if err := rows.Err(); err != nil {
		return nil, err
	}

	return items, nil
}

// RecordCredentialUse runs the :execrows query against sqlite.
func (q *sqliteQueries) RecordCredentialUse(ctx context.Context, db DBTX, arg RecordCredentialUseParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.recordCredentialUse,
		arg.SignCount,
		timeTextPtr(arg.LastUsedAt),
		arg.ID,
		arg.Scope,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// Shape assertions.
//
// Each conversion below compiles only if the shared type still has exactly
// these fields, with these types, in this order — which is what makes a
// disagreement between dialects a compile error here rather than a
// transposition at run time.
var (
	_ = struct {
		ID            string
		Scope         tenancy.Scope
		BelongsToUser string
	}(ArchiveCredentialForUserParams{})
	_ = struct {
		ID            string
		Scope         tenancy.Scope
		BelongsToUser string
		CredentialID  []byte
		PublicKey     []byte
		Transports    string
		FriendlyName  string
		SignCount     int64
	}(CreateCredentialParams{})
	_ = struct {
		Scope         tenancy.Scope
		BelongsToUser string
	}(DeleteCredentialsForUserParams{})
	_ = struct {
		ID    string
		Scope tenancy.Scope
	}(GetArchivedCredentialParams{})
	_ = struct {
		ID            string
		Scope         tenancy.Scope
		BelongsToUser string
		CredentialID  []byte
		PublicKey     []byte
		Transports    string
		FriendlyName  string
		SignCount     int64
		CreatedAt     time.Time
		LastUpdatedAt *time.Time
		LastUsedAt    *time.Time
		ArchivedAt    *time.Time
	}(GetArchivedCredentialRow{})
	_ = struct {
		ID    string
		Scope tenancy.Scope
	}(GetCredentialParams{})
	_ = struct {
		ID            string
		Scope         tenancy.Scope
		BelongsToUser string
		CredentialID  []byte
		PublicKey     []byte
		Transports    string
		FriendlyName  string
		SignCount     int64
		CreatedAt     time.Time
		LastUpdatedAt *time.Time
		LastUsedAt    *time.Time
		ArchivedAt    *time.Time
	}(GetCredentialRow{})
	_ = struct {
		CredentialID []byte
		Scope        tenancy.Scope
	}(GetCredentialByCredentialIDParams{})
	_ = struct {
		ID            string
		Scope         tenancy.Scope
		BelongsToUser string
		CredentialID  []byte
		PublicKey     []byte
		Transports    string
		FriendlyName  string
		SignCount     int64
		CreatedAt     time.Time
		LastUpdatedAt *time.Time
		LastUsedAt    *time.Time
		ArchivedAt    *time.Time
	}(GetCredentialByCredentialIDRow{})
	_ = struct {
		Scope         tenancy.Scope
		BelongsToUser string
	}(ListCredentialsForUserParams{})
	_ = struct {
		ID            string
		Scope         tenancy.Scope
		BelongsToUser string
		CredentialID  []byte
		PublicKey     []byte
		Transports    string
		FriendlyName  string
		SignCount     int64
		CreatedAt     time.Time
		LastUpdatedAt *time.Time
		LastUsedAt    *time.Time
		ArchivedAt    *time.Time
	}(ListCredentialsForUserRow{})
	_ = struct {
		Scope tenancy.Scope
		Users []string
	}(ListCredentialsForUsersParams{})
	_ = struct {
		ID            string
		Scope         tenancy.Scope
		BelongsToUser string
		CredentialID  []byte
		PublicKey     []byte
		Transports    string
		FriendlyName  string
		SignCount     int64
		CreatedAt     time.Time
		LastUpdatedAt *time.Time
		LastUsedAt    *time.Time
		ArchivedAt    *time.Time
	}(ListCredentialsForUsersRow{})
	_ = struct {
		SignCount  int64
		LastUsedAt *time.Time
		ID         string
		Scope      tenancy.Scope
	}(RecordCredentialUseParams{})
)
