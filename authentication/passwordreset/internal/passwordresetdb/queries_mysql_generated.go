// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package passwordresetdb

import (
	"context"
	"strings"
	"time"

	"github.com/primandproper/primitives-go/v2/tenancy"
)

const getTokenByDigestMySQL = `SELECT
	{{prefix}}password_reset_tokens.id,
	{{prefix}}password_reset_tokens.scope,
	{{prefix}}password_reset_tokens.belongs_to_user,
	{{prefix}}password_reset_tokens.expires_at,
	{{prefix}}password_reset_tokens.redeemed_at,
	{{prefix}}password_reset_tokens.created_at
FROM {{prefix}}password_reset_tokens
WHERE {{prefix}}password_reset_tokens.token_digest = ?
	AND {{prefix}}password_reset_tokens.scope = ?`

const insertTokenMySQL = `
INSERT INTO {{prefix}}password_reset_tokens (
	id,
	scope,
	belongs_to_user,
	token_digest,
	expires_at,
	created_at
) VALUES (
	?,
	?,
	?,
	?,
	?,
	?
)`

const redeemTokenMySQL = `UPDATE {{prefix}}password_reset_tokens SET
	redeemed_at = ?
WHERE id = ?
	AND redeemed_at IS NULL`

const revokeTokensForUserMySQL = `DELETE FROM {{prefix}}password_reset_tokens
WHERE scope = ?
	AND belongs_to_user = ?
	AND redeemed_at IS NULL`

const sweepExpiredTokensMySQL = `DELETE FROM {{prefix}}password_reset_tokens
WHERE expires_at <= ?`

// mysqlQueries answers every query in Querier against mysql.
type mysqlQueries struct {
	getTokenByDigest    string
	insertToken         string
	redeemToken         string
	revokeTokensForUser string
	sweepExpiredTokens  string
}

// newMySQL returns the mysql querier with prefix substituted into every
// table name the analyzer identified.
func newMySQL(prefix string) *mysqlQueries {
	return &mysqlQueries{
		getTokenByDigest:    strings.ReplaceAll(getTokenByDigestMySQL, prefixMarker, prefix),
		insertToken:         strings.ReplaceAll(insertTokenMySQL, prefixMarker, prefix),
		redeemToken:         strings.ReplaceAll(redeemTokenMySQL, prefixMarker, prefix),
		revokeTokensForUser: strings.ReplaceAll(revokeTokensForUserMySQL, prefixMarker, prefix),
		sweepExpiredTokens:  strings.ReplaceAll(sweepExpiredTokensMySQL, prefixMarker, prefix),
	}
}

// GetTokenByDigest runs the :one query against mysql.
func (q *mysqlQueries) GetTokenByDigest(ctx context.Context, db DBTX, arg GetTokenByDigestParams) (GetTokenByDigestRow, error) {
	row := db.QueryRowContext(ctx, q.getTokenByDigest,
		arg.TokenDigest,
		arg.Scope,
	)

	var i GetTokenByDigestRow

	err := row.Scan(
		&i.ID,
		&i.Scope,
		&i.BelongsToUser,
		&i.ExpiresAt,
		&i.RedeemedAt,
		&i.CreatedAt,
	)

	return i, err
}

// InsertToken runs the :exec query against mysql.
func (q *mysqlQueries) InsertToken(ctx context.Context, db DBTX, arg InsertTokenParams) error {
	_, err := db.ExecContext(ctx, q.insertToken,
		arg.ID,
		arg.Scope,
		arg.BelongsToUser,
		arg.TokenDigest,
		arg.ExpiresAt,
		arg.CreatedAt,
	)

	return err
}

// RedeemToken runs the :execrows query against mysql.
func (q *mysqlQueries) RedeemToken(ctx context.Context, db DBTX, arg RedeemTokenParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.redeemToken,
		arg.RedeemedAt,
		arg.ID,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// RevokeTokensForUser runs the :execrows query against mysql.
func (q *mysqlQueries) RevokeTokensForUser(ctx context.Context, db DBTX, arg RevokeTokensForUserParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.revokeTokensForUser,
		arg.Scope,
		arg.BelongsToUser,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// SweepExpiredTokens runs the :execrows query against mysql.
func (q *mysqlQueries) SweepExpiredTokens(ctx context.Context, db DBTX, arg SweepExpiredTokensParams) (int64, error) {
	result, err := db.ExecContext(ctx, q.sweepExpiredTokens,
		arg.ExpiresBefore,
	)
	if err != nil {
		return 0, err
	}

	return result.RowsAffected()
}

// Shape assertions.
//
// Each conversion below compiles only if the shared type still has exactly
// these fields, with these types, in this order — which is what makes a
// disagreement between dialects a compile error here rather than a
// transposition at run time.
var (
	_ = struct {
		TokenDigest string
		Scope       tenancy.Scope
	}(GetTokenByDigestParams{})
	_ = struct {
		ID            string
		Scope         tenancy.Scope
		BelongsToUser string
		ExpiresAt     time.Time
		RedeemedAt    *time.Time
		CreatedAt     time.Time
	}(GetTokenByDigestRow{})
	_ = struct {
		ID            string
		Scope         tenancy.Scope
		BelongsToUser string
		TokenDigest   string
		ExpiresAt     time.Time
		CreatedAt     time.Time
	}(InsertTokenParams{})
	_ = struct {
		RedeemedAt *time.Time
		ID         string
	}(RedeemTokenParams{})
	_ = struct {
		Scope         tenancy.Scope
		BelongsToUser string
	}(RevokeTokensForUserParams{})
	_ = struct {
		ExpiresBefore time.Time
	}(SweepExpiredTokensParams{})
)
