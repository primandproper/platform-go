// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package passwordresetdb

import (
	"context"
)

// Querier is every query in this package, with one signature per query
// regardless of which dialect answers it.
//
// That is the point of this package: the params type, the row type, and the
// method signature are the same on every dialect, so nothing above this line
// knows or cares which database is underneath.
//
// # A note on the :execrows count
//
// MySQL reports rows *changed*: an UPDATE that sets a column to the value it
// already held affects zero rows there. Postgres and SQLite report rows
// *matched*, and count that same UPDATE as one.
//
// So a statement that gates on the count — treating zero as "not found" — is
// correct on two engines and wrong on the third. Either give it a predicate
// that discriminates, or set clientFoundRows=true in the MySQL DSN, which
// switches MySQL to matched semantics.
type Querier interface {
	// DeleteTokensForUser runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	DeleteTokensForUser(ctx context.Context, db DBTX, arg DeleteTokensForUserParams) (int64, error)
	// GetTokenByDigest runs the :one query.
	GetTokenByDigest(ctx context.Context, db DBTX, arg GetTokenByDigestParams) (GetTokenByDigestRow, error)
	// InsertToken runs the :exec query.
	InsertToken(ctx context.Context, db DBTX, arg InsertTokenParams) error
	// ListTokensForUser runs the :many query.
	ListTokensForUser(ctx context.Context, db DBTX, arg ListTokensForUserParams) ([]ListTokensForUserRow, error)
	// RedeemToken runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RedeemToken(ctx context.Context, db DBTX, arg RedeemTokenParams) (int64, error)
	// RevokeTokensForUser runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RevokeTokensForUser(ctx context.Context, db DBTX, arg RevokeTokensForUserParams) (int64, error)
	// SweepExpiredTokens runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	SweepExpiredTokens(ctx context.Context, db DBTX, arg SweepExpiredTokensParams) (int64, error)
}
