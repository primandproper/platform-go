// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package registrydb

import (
	"context"
)

// Querier is every query in this package, with one signature per query
// regardless of which dialect answers it.
//
// That is the point of this package: the params type, the row type, and the
// method signature are the same on every dialect, so nothing above this line
// knows or cares which database is underneath.
//
// # A note on the :execrows count
//
// MySQL reports rows *changed*: an UPDATE that sets a column to the value it
// already held affects zero rows there. Postgres and SQLite report rows
// *matched*, and count that same UPDATE as one.
//
// So a statement that gates on the count — treating zero as "not found" — is
// correct on two engines and wrong on the third. Either give it a predicate
// that discriminates, or set clientFoundRows=true in the MySQL DSN, which
// switches MySQL to matched semantics.
type Querier interface {
	// ArchiveObject runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ArchiveObject(ctx context.Context, db DBTX, arg ArchiveObjectParams) (int64, error)
	// CreateObject runs the :exec query.
	CreateObject(ctx context.Context, db DBTX, arg CreateObjectParams) error
	// GetObject runs the :one query.
	GetObject(ctx context.Context, db DBTX, arg GetObjectParams) (GetObjectRow, error)
	// GetObjectByKey runs the :one query.
	GetObjectByKey(ctx context.Context, db DBTX, arg GetObjectByKeyParams) (GetObjectByKeyRow, error)
	// GetObjectCreatedAt runs the :one query.
	GetObjectCreatedAt(ctx context.Context, db DBTX, arg GetObjectCreatedAtParams) (GetObjectCreatedAtRow, error)
	// GetObjectIDByKey runs the :one query.
	GetObjectIDByKey(ctx context.Context, db DBTX, arg GetObjectIDByKeyParams) (GetObjectIDByKeyRow, error)
	// ListObjects runs the :many query.
	ListObjects(ctx context.Context, db DBTX, arg ListObjectsParams) ([]ListObjectsRow, error)
	// ListObjectsByOwner runs the :many query.
	ListObjectsByOwner(ctx context.Context, db DBTX, arg ListObjectsByOwnerParams) ([]ListObjectsByOwnerRow, error)
	// ListObjectsByOwnerDescending runs the :many query.
	ListObjectsByOwnerDescending(ctx context.Context, db DBTX, arg ListObjectsByOwnerDescendingParams) ([]ListObjectsByOwnerDescendingRow, error)
	// ListObjectsBySubject runs the :many query.
	ListObjectsBySubject(ctx context.Context, db DBTX, arg ListObjectsBySubjectParams) ([]ListObjectsBySubjectRow, error)
	// ListObjectsBySubjectDescending runs the :many query.
	ListObjectsBySubjectDescending(ctx context.Context, db DBTX, arg ListObjectsBySubjectDescendingParams) ([]ListObjectsBySubjectDescendingRow, error)
	// ListObjectsDescending runs the :many query.
	ListObjectsDescending(ctx context.Context, db DBTX, arg ListObjectsDescendingParams) ([]ListObjectsDescendingRow, error)
}
