-- Code generated by github.com/primandproper/platform-go/v14/mediaregistry/internal/queriesgen. DO NOT EDIT.

-- name: CreateObject :exec
INSERT INTO uploads_objects (
	id,
	scope,
	object_key,
	content_type,
	size_bytes,
	owner_id,
	belongs_to_type,
	belongs_to_id
) VALUES (
	sqlc.arg(id),
	sqlc.arg(scope),
	sqlc.arg(object_key),
	sqlc.arg(content_type),
	sqlc.arg(size_bytes),
	sqlc.arg(owner_id),
	sqlc.arg(belongs_to_type),
	sqlc.arg(belongs_to_id)
);

-- name: GetObject :one
SELECT
	uploads_objects.id,
	uploads_objects.scope,
	uploads_objects.object_key,
	uploads_objects.content_type,
	uploads_objects.size_bytes,
	uploads_objects.owner_id,
	uploads_objects.belongs_to_type,
	uploads_objects.belongs_to_id,
	uploads_objects.created_at,
	uploads_objects.last_updated_at,
	uploads_objects.archived_at
FROM uploads_objects
WHERE uploads_objects.archived_at IS NULL
	AND uploads_objects.id = sqlc.arg(id)
	AND uploads_objects.scope = sqlc.arg(scope);

-- name: ListObjects :many
SELECT
	uploads_objects.id,
	uploads_objects.scope,
	uploads_objects.object_key,
	uploads_objects.content_type,
	uploads_objects.size_bytes,
	uploads_objects.owner_id,
	uploads_objects.belongs_to_type,
	uploads_objects.belongs_to_id,
	uploads_objects.created_at,
	uploads_objects.last_updated_at,
	uploads_objects.archived_at,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
	) AS total_count
FROM uploads_objects
WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
	AND uploads_objects.scope = sqlc.arg(scope)
	AND uploads_objects.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY uploads_objects.id ASC
LIMIT ?;

-- name: ListObjectsDescending :many
SELECT
	uploads_objects.id,
	uploads_objects.scope,
	uploads_objects.object_key,
	uploads_objects.content_type,
	uploads_objects.size_bytes,
	uploads_objects.owner_id,
	uploads_objects.belongs_to_type,
	uploads_objects.belongs_to_id,
	uploads_objects.created_at,
	uploads_objects.last_updated_at,
	uploads_objects.archived_at,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
	) AS filtered_count,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
	) AS total_count
FROM uploads_objects
WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
	AND uploads_objects.scope = sqlc.arg(scope)
	AND (uploads_objects.id <= COALESCE(sqlc.narg(page_cursor), uploads_objects.id) AND uploads_objects.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY uploads_objects.id DESC
LIMIT ?;

-- name: ArchiveObject :execrows
UPDATE uploads_objects SET
	archived_at = CURRENT_TIMESTAMP(6)
WHERE archived_at IS NULL
	AND id = sqlc.arg(id)
	AND scope = sqlc.arg(scope);

-- name: ListObjectsByOwner :many
SELECT
	uploads_objects.id,
	uploads_objects.scope,
	uploads_objects.object_key,
	uploads_objects.content_type,
	uploads_objects.size_bytes,
	uploads_objects.owner_id,
	uploads_objects.belongs_to_type,
	uploads_objects.belongs_to_id,
	uploads_objects.created_at,
	uploads_objects.last_updated_at,
	uploads_objects.archived_at,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
			AND uploads_objects.owner_id = sqlc.arg(owner_id)
	) AS filtered_count,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
			AND uploads_objects.owner_id = sqlc.arg(owner_id)
	) AS total_count
FROM uploads_objects
WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
	AND uploads_objects.scope = sqlc.arg(scope)
	AND uploads_objects.owner_id = sqlc.arg(owner_id)
	AND uploads_objects.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY uploads_objects.id ASC
LIMIT ?;

-- name: ListObjectsByOwnerDescending :many
SELECT
	uploads_objects.id,
	uploads_objects.scope,
	uploads_objects.object_key,
	uploads_objects.content_type,
	uploads_objects.size_bytes,
	uploads_objects.owner_id,
	uploads_objects.belongs_to_type,
	uploads_objects.belongs_to_id,
	uploads_objects.created_at,
	uploads_objects.last_updated_at,
	uploads_objects.archived_at,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
			AND uploads_objects.owner_id = sqlc.arg(owner_id)
	) AS filtered_count,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
			AND uploads_objects.owner_id = sqlc.arg(owner_id)
	) AS total_count
FROM uploads_objects
WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
	AND uploads_objects.scope = sqlc.arg(scope)
	AND uploads_objects.owner_id = sqlc.arg(owner_id)
	AND (uploads_objects.id <= COALESCE(sqlc.narg(page_cursor), uploads_objects.id) AND uploads_objects.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY uploads_objects.id DESC
LIMIT ?;

-- name: ListObjectsBySubject :many
SELECT
	uploads_objects.id,
	uploads_objects.scope,
	uploads_objects.object_key,
	uploads_objects.content_type,
	uploads_objects.size_bytes,
	uploads_objects.owner_id,
	uploads_objects.belongs_to_type,
	uploads_objects.belongs_to_id,
	uploads_objects.created_at,
	uploads_objects.last_updated_at,
	uploads_objects.archived_at,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
			AND uploads_objects.belongs_to_type = sqlc.arg(belongs_to_type)
			AND uploads_objects.belongs_to_id = sqlc.arg(belongs_to_id)
	) AS filtered_count,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
			AND uploads_objects.belongs_to_type = sqlc.arg(belongs_to_type)
			AND uploads_objects.belongs_to_id = sqlc.arg(belongs_to_id)
	) AS total_count
FROM uploads_objects
WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
	AND uploads_objects.scope = sqlc.arg(scope)
	AND uploads_objects.belongs_to_type = sqlc.arg(belongs_to_type)
	AND uploads_objects.belongs_to_id = sqlc.arg(belongs_to_id)
	AND uploads_objects.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY uploads_objects.id ASC
LIMIT ?;

-- name: ListObjectsBySubjectDescending :many
SELECT
	uploads_objects.id,
	uploads_objects.scope,
	uploads_objects.object_key,
	uploads_objects.content_type,
	uploads_objects.size_bytes,
	uploads_objects.owner_id,
	uploads_objects.belongs_to_type,
	uploads_objects.belongs_to_id,
	uploads_objects.created_at,
	uploads_objects.last_updated_at,
	uploads_objects.archived_at,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
			)
			AND (
				uploads_objects.last_updated_at IS NULL
				OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
			)
			AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
			AND uploads_objects.belongs_to_type = sqlc.arg(belongs_to_type)
			AND uploads_objects.belongs_to_id = sqlc.arg(belongs_to_id)
	) AS filtered_count,
	(
		SELECT COUNT(uploads_objects.id)
		FROM uploads_objects
		WHERE (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
			AND uploads_objects.scope = sqlc.arg(scope)
			AND uploads_objects.belongs_to_type = sqlc.arg(belongs_to_type)
			AND uploads_objects.belongs_to_id = sqlc.arg(belongs_to_id)
	) AS total_count
FROM uploads_objects
WHERE uploads_objects.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	AND uploads_objects.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - INTERVAL 999 YEAR))
	)
	AND (
		uploads_objects.last_updated_at IS NULL
		OR uploads_objects.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + INTERVAL 999 YEAR))
	)
	AND (COALESCE(sqlc.narg(include_archived), false) = true OR uploads_objects.archived_at IS NULL)
	AND uploads_objects.scope = sqlc.arg(scope)
	AND uploads_objects.belongs_to_type = sqlc.arg(belongs_to_type)
	AND uploads_objects.belongs_to_id = sqlc.arg(belongs_to_id)
	AND (uploads_objects.id <= COALESCE(sqlc.narg(page_cursor), uploads_objects.id) AND uploads_objects.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY uploads_objects.id DESC
LIMIT ?;

-- name: GetObjectByKey :one
SELECT
	uploads_objects.id,
	uploads_objects.scope,
	uploads_objects.object_key,
	uploads_objects.content_type,
	uploads_objects.size_bytes,
	uploads_objects.owner_id,
	uploads_objects.belongs_to_type,
	uploads_objects.belongs_to_id,
	uploads_objects.created_at,
	uploads_objects.last_updated_at,
	uploads_objects.archived_at
FROM uploads_objects
WHERE uploads_objects.archived_at IS NULL
	AND uploads_objects.object_key = sqlc.arg(object_key)
	AND uploads_objects.scope = sqlc.arg(scope);

-- name: GetObjectIDByKey :one
SELECT
	uploads_objects.id
FROM uploads_objects
WHERE uploads_objects.object_key = sqlc.arg(object_key)
	AND uploads_objects.scope = sqlc.arg(scope);

-- name: GetObjectCreatedAt :one
SELECT
	uploads_objects.created_at
FROM uploads_objects
WHERE uploads_objects.id = sqlc.arg(id)
	AND uploads_objects.scope = sqlc.arg(scope);
